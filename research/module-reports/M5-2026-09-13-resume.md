# M5 recovered-worktree checkpoint — 2026-09-13

## Authority and recovered identity

The author authorized continuing unfinished M5 and explicitly approved the
malformed Docker image/inspect response repair. The restored worktree is
`.worktrees/m5-validation-swap`, branch `codex/m5-validation-swap`, clean at
`ea68558` on resume. The previously missing directory is now present; its
disappearance is retained as an environment interruption, not a passing run.

Recovered commits are `03d9ba4` (restricted stage), `6e798c7` (Docker response
repair together with unfinished twin code), `de1496e` (three source/attempt
tests), and `ea68558` (source identity adjustment). Commit subjects are not
acceptance evidence: neither a complete M5-4 report nor a completed independent
repair review was available at resume. Earlier partial work remains in history.
The mixed contents of `6e798c7` are preserved, not rewritten or represented as
a narrowly isolated fix commit.

## Verified baseline

The main agent read AGENTS.md, the full frozen specification and execution
plan, accepted ADR-0005, the derived M5 plan, current decisions and task briefs.
Both frozen SHA-256 values match:

- CCS-2.1: `aaddaa8def8df1ef2efbe4f9487de0ba86a60ae5aed8955ca76f5e070fa01e5c`.
- Frozen plan: `7c1feebb36776c09de5c0ba5462540072374b9902d8e04d405585a8efb8659c0`.
- Accepted ADR-0005: `c4457f5da29903261ea7ce58c68e28c43cbdfb74eb55e70a8ff5c1d1617921a0`.

Relative to `66d6ca7`, the frozen documents, ADR, schemas, dependencies,
M1-M4 code and M5-1/2/3 implementation are unchanged. M5-1/2/3 retain their
previous independent acceptance. M5-4 and subsequent tasks remain unfinished.

## Fresh checks and current limits

At `ea68558`, ordinary and no-ignore Ruff default checks pass. Mypy passes
for the actual 64 source files; the separate validate/swap check covers 24.
Offline lock verification resolves the existing 46 packages without changes.
`git diff --check` passes. Both complexity scans fail on the unfinished
`swap/source.py::git_tree`: C901 15 > 10 and PLR0912 14 > 12. A command sequence
whose last command succeeds does not turn these earlier failures into passes.
The source completion must address them without relaxing thresholds.

Fresh `uv run --locked --offline pytest -q` passes **974 tests in 86.23s** at
`ea68558`. This includes the inherited tests and current stage/source/attempt
checks. The independent scoped Docker response re-review is still in progress;
no independent PASS is claimed yet.
No test here executes the complete prepare/activate/rollback path: its required
integration and security files have not yet been implemented.

## Continuation boundary

Finish the already-approved response repair and scoped independent verification,
then complete M5-4's same-executor, view-only twin orchestration with real stage
records and authenticated attempts. Preserve source/checker isolation, current
permission checks, fixed repetitions, negative outcomes and all existing tests.
M5-5 supplies the runtime state owner; M5-6 performs activation and rollback;
M5-7 performs complete verification and independent exit audit. Newly blocking
independent findings still require the author's itemized repair approval.

This is C2/C5 engineering recovery evidence with prospective RQ3 relevance;
it is not C3/C4, human benchmark truth or formal empirical evidence. Formal
project progress remains M0-M4, 5/12 (41.7%). Original G1/G2/G3 dates remain
missed; no frozen schedule change or gate backdating is implied.

**M5 is not complete. M6 has not started.**

## Scoped review continuation

The independent reviewer reports one residual within the already approved
malformed-response repair: deeply nested valid JSON (10,000 nested arrays,
about 20 KB) raises raw `RecursionError` in both image and state decoding.
The normal/missing/type matrix and real cleanup diagnostics passed, but this
residual means the scoped verdict is REQUEST CHANGES, not PASS.

The author's existing repair authorization covers this same parser failure
class. A narrow implementer is adding failing tests and the decoder fix;
the same independent reviewer will verify the resulting exact commit.
This does not authorize unrelated repairs or expand any frozen interface.

The implementation worker twice encountered native429 before source edits.
Main completed the narrow approved repair at `d5ad681`: two decoder exception
tuples now normalize RecursionError; five regressions cover direct image/state
decoding, image preparation and actual initializer/subject cleanup. All five
failed on the original raw exception, then passed; stage suite38 passed and
full979 passed in84.52s. The original independent REQUEST CHANGES report and
diagnostics are archived under `M5-4A-independent-rereview.md` and
`M5-4A-verification/`.

Independent exact-commit re-review now marks the prior finding ADDRESSED and
scoped specification/quality PASS at `d5ad681`. It reran nine original/new
malformed-response regressions (including actual Docker cleanup), independently
checked the exact stable exceptions, and passed scoped lint/complexity/types.
Its report is `M5-4A-recursion-independent-rereview.md`, SHA-256
`2273df758b54789e3aeb04aee28d4e70e64e7d299a32528d3331394c48b226c5`.
This closes the already-approved parser repair; the remaining M5-4B task
continues under the existing module authorization. No additional approval
was inferred for unrelated findings, M5 exit or M6.
