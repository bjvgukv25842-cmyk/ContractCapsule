# M3 Module Report

## Status and Authorization

- Authorized module: **M3 -- Builder, source-map, trust classification, and
  quarantine**.
- Technical status: implementation and verification complete; the M3 human
  exit gate remains **pending author review**.
- Input baseline: `a0dc9c72b210e41699aad6377a21642cac75abf0` (the M2 final
  governance HEAD).
- Branch: `codex/m3-builder`.
- Isolated worktree:
  `/Users/litmus/Documents/Contract Capsule/.worktrees/m3-builder`.
- Technical commit: `232b475ce302e06e9292362ffb12e530a5672548` (`feat: implement
  M3 source-grounded builder`).
- Governance evidence commit: `a499e12373e2cde8e0802bd501035aa7ac36458b`
  (`docs: record M3 builder evidence`). The final verification addendum in
  this report is recorded by a subsequent local documentation commit.
- No push, merge, pull, PR, branch deletion, or history rewriting was done.
- M4 and all later modules were not started.

The frozen M3 window is 2026-08-13--2026-08-16. The author explicitly
authorized work to start on 2026-08-09. This is recorded as **schedule
acceleration without semantic scope change**; no frozen requirement, safety
gate, TDD rule, or human-exit condition was waived.

## Governing Inputs and Skills

The implementation was checked against the repository instructions and the
complete frozen baselines:

- `AGENTS.md`
- `docs/spec/CCS-2.1.md`
- `docs/superpowers/plans/2026-07-30-contract-capsule-fse-2027-execution-plan.md`
- the approved M0 and M1 reports, protocol, claim matrix, decision log, and
  AI ledger
- the approved M2 models, package loader, CAS, Registry, tests, and lock file

The registered curated instructions were read completely and used:

- `superpowers:executing-plans`
- `superpowers:using-git-worktrees`
- `superpowers:test-driven-development`
- `superpowers:verification-before-completion`

No unavailable Skill was claimed, installed, or substituted with untrusted
code.

## Delivered Files

| File | Role |
|---|---|
| `src/contractcapsule/build/ingest.py` | strict source inputs, local snapshots, Git/CAS/external provenance, parser selection, source-drift checks, and pre-storage secret scanning |
| `src/contractcapsule/build/atomize.py` | deterministic Markdown, Python AST, JSON/YAML, and text candidate extraction; exact span binding and rendering |
| `src/contractcapsule/build/publish.py` | validated-atom assembly, package materialization, mandatory public `load_capsule()` validation, and M2 Registry publication |
| `src/contractcapsule/audit/quarantine.py` | candidate/evidence quarantine, trust labels, approval verification adapter, promotion, and revalidation |
| `src/contractcapsule/build/__init__.py` | public M3 builder exports |
| `src/contractcapsule/audit/__init__.py` | public quarantine exports |
| `tests/integration/test_build_pipeline.py` | deterministic parsing, provenance, drift, build/load/publish, and evidence-preservation tests |
| `tests/security/test_trust_promotion.py` | generated-candidate, approval, evidence, secret, and bypass fault-injection tests |

## Frozen Interfaces

The required signatures are available through the public modules:

```python
snapshot_source(source: SourceInput, principal: Principal) -> SourceSnapshot
extract_candidate_atoms(snapshot: SourceSnapshot) -> list[CandidateAtom]
bind_evidence(atom: CandidateAtom, snapshot: SourceSnapshot) -> EvidenceBinding
promote(candidate_id: str, approval: HumanApproval) -> ValidatedAtom
build_capsule(build_request: BuildRequest) -> DraftCapsule
```

`promote` is exposed by `contractcapsule.audit.quarantine` and uses an
explicitly configured process-local `QuarantineStore`; the store defaults to a
deny-all approval verifier. `publish_draft` is an additional builder boundary
that accepts only a loader-issued draft receipt and delegates publication to
the M2 `Registry`.

## Implementation Evidence

### Ingestion and source fidelity

- `SourceInput` supports the three CCS-2.1 evidence modes: `CAS`,
  `GIT_IMMUTABLE`, and `EXTERNAL_IMMUTABLE`.
- Local paths are absolute, regular, non-symlink files contained by a
  repository root and represented by a literal repository-relative path.
- Git mode requires a canonical HTTPS repository identity, a full algorithm-
  tagged commit OID, a matching local `origin`, and bytes equal to
  `git show <commit>:<path>`. No network fetch is attempted.
- External mode accepts an offline byte fixture plus a canonical HTTPS, DOI, or
  URN locator, independent content digest, source, and verification method. It
  does not silently fall back to CAS.
- Snapshots retain exact UTF-8 bytes, content digest, capture metadata,
  repository/commit/path or external locator, parser kind, and the requesting
  principal. Snapshot identity excludes capture time so repeated identical
  inputs receive the same ID.
- Strict JSON rejects duplicate keys and non-finite constants. Strict YAML
  rejects duplicate keys, aliases, anchors, and tags. Invalid UTF-8 and lone
  surrogates fail closed.

### Deterministic atomization and source-map

- Markdown headings and bodies are parsed without an LLM; Python symbols use
  `ast`; configuration keys use strict JSON/YAML parsing; other sources use
  stable non-empty line spans.
- Candidate IDs are derived from snapshot ID, statement, kind, exact line
  span, and stable symbol/heading. Repeated extraction is deterministic.
- Every candidate is registered in the quarantine store and carries a private
  extractor token. Generated candidates (`generated=True`) are always `T3` and
  `candidate`; ordinary deterministic source candidates are classified `T2`
  and remain candidates until approval.
- Evidence bindings include mode, content/blob digest, span digest, media type,
  access policy, source snapshot, and stable source-map fields. Git bindings
  include repository URI, full immutable commit, path, symbol/heading, and
  line span. Binding and render operations recheck source bytes and span
  digests, so retaining an old line number cannot hide source drift.

### Quarantine and promotion

- The append-only process-local store accepts snapshots and candidates only from
  ingestion/extraction capabilities; callers cannot register an arbitrary
  object through the public constructors alone.
- Promotion is fail-closed when evidence is missing, the candidate or binding
  digest does not match, the source has drifted, the approval is rejected,
  forged, expired, future-dated, self-issued, or not verified by the injected
  trusted authority.
- A successful promotion emits an immutable `ValidatedAtom` with final trust
  `T1`, status `validated`, exact evidence bindings, and the matching approval.
- The approval implementation is an injectable HMAC test/research adapter. It
  is explicitly not a production signature-security claim; the default store
  has no implicit signing authority and denies approval.
- Secret detection runs before snapshot/CAS persistence, binding, promotion,
  package writing, and evidence rendering. Detection failures block the path.

### Build, validation, and publication boundary

- `build_capsule` accepts only store-emitted `ValidatedAtom` values and
  revalidates each one through the same store before constructing the seven
  CCS-2.1 modules. Raw candidates and forged/replaced validated values are
  rejected.
- The builder emits source-map and approval metadata as extension data while
  preserving the M2 core model. It does not resolve eligibility, rank, compute
  dependency closure, resolve conflicts, compile a view, activate a version,
  or roll back a version.
- A temporary package is written, checked by the public M2 `load_capsule`
  entry point (strict parsing, Schema, Python semantics, digest and reference
  integrity), and only then returned as a `DraftCapsule`.
- `publish_draft` requires the private loader receipt and delegates to M2
  `Registry.publish`, retaining M2 lifecycle, CAS, authorization, immutable
  version, and transaction semantics. There is no internal publication
  bypass.

## Trust Transition

| State | Entry condition | Allowed output |
|---|---|---|
| `T3/candidate` | generated or unverified candidate extracted into quarantine | no published payload |
| `T2/candidate` | deterministic source candidate without human approval | no published payload |
| `T1/validated` | exact evidence binding plus externally verified human approval | eligible input to M3 builder only |
| published P0/P1 | M2 loader and Registry revalidation after M3 assembly | immutable published Capsule |

The implementation intentionally does not provide an autonomous promotion path.

## TDD Red to Green Record

The first focused test run was executed before the M3 modules existed:

```text
uv run pytest tests/integration/test_build_pipeline.py tests/security/test_trust_promotion.py -v
Exit 2; collected 0; collection failed with ModuleNotFoundError for
contractcapsule.audit (both requested test modules).
```

After the minimum ingestion, atomization, quarantine, and publication
implementation was added, the focused suite was rerun:

```text
uv run pytest tests/integration/test_build_pipeline.py tests/security/test_trust_promotion.py -v -ra
Exit 0; 28 passed in 1.39s; no skipped, xfailed, or deselected tests.
```

Representative Red-to-Green mappings are:

| Failure obligation | Persistent test |
|---|---|
| deterministic source/atom extraction | `test_snapshot_and_atomization_are_deterministic`; `test_code_config_and_schema_use_deterministic_structural_parsers` |
| duplicate structured-source keys | `test_source_structured_parsers_reject_duplicate_keys` |
| immutable Git locator and source drift | `test_git_binding_contains_commit_path_stable_heading_and_digests`; `test_source_drift_is_rejected_even_when_old_line_numbers_are_reused` |
| offline external evidence | `test_external_immutable_source_is_offline_but_fully_bound` |
| T3 quarantine and no self-promotion | `test_generated_observation_is_quarantined`; `test_approval_cannot_be_self_issued_without_trusted_authority` |
| missing/forged/expired approval or evidence | `test_validated_atom_requires_binding_and_external_approval`; `test_forged_or_mismatched_approval_fails_closed`; `test_approval_signature_tampering_fails_closed`; `test_expired_approval_fails_closed`; `test_missing_evidence_cannot_promote_p0_or_p1` |
| evidence cannot be replaced by a summary | `test_summary_cannot_replace_evidence` |
| loader/public-publish boundary | `test_public_build_validate_publish_pipeline_has_no_draft_bypass`; `test_tampered_validated_atom_cannot_bypass_quarantine` |
| secret storage/rendering boundary | `test_secret_content_is_rejected_before_storage_or_rendering`; `test_secret_scan_is_repeated_before_evidence_rendering` |

## Verification Record

The implementation commands below were run on technical HEAD
`232b475ce302e06e9292362ffb12e530a5672548`; the focused, cumulative, static,
lock, complexity, whitespace, ledger, and frozen-input checks were rerun on
the final governance HEAD `a499e12373e2cde8e0802bd501035aa7ac36458b` with the
same results.

| Command | Result |
|---|---|
| `uv run pytest tests/integration/test_build_pipeline.py tests/security/test_trust_promotion.py -v -ra` | Exit 0; 28 passed |
| `uv run pytest -q -ra` | Exit 0; 280 passed |
| `uv run pytest tests/fixtures/formal_cases -q -ra` | Exit 0; 30 passed |
| `uv run ruff check .` | Exit 0; all checks passed |
| `uv run mypy .` | Exit 0; no issues in 32 source files |
| `uv lock --check` | Exit 0; 38 packages resolved |
| `uv run ruff check src/contractcapsule tests --select C901,PLR0911,PLR0912,PLR0915` | Exit 0 |
| `git diff --cached --check` before technical commit | Exit 0 |

Final governance-HEAD checks:

| Command | Result |
|---|---|
| `uv run pytest tests/integration/test_build_pipeline.py tests/security/test_trust_promotion.py -v -ra` | Exit 0; 28 passed in 1.45s |
| `uv run pytest -q -ra` | Exit 0; 280 passed in 2.88s |
| `uv run pytest tests/unit/test_models.py tests/unit/test_package_loader.py tests/unit/test_schema_python_parity.py tests/unit/test_cas.py tests/unit/test_registry.py tests/property/test_immutability.py -q -ra` | Exit 0; 218 passed in 1.80s |
| `uv run pytest tests/fixtures/formal_cases -q -ra` | Exit 0; 30 passed in 0.15s |
| `uv run pytest tests/unit/test_spec_lock.py -q -ra` | Exit 0; 4 passed in 0.22s |
| `uv run ruff check .` | Exit 0 |
| `uv run mypy .` | Exit 0; no issues in 32 source files |
| `uv lock --check` | Exit 0; 38 packages resolved |
| `uv run ruff check src/contractcapsule tests --select C901,PLR0911,PLR0912,PLR0915` | Exit 0 |
| `git diff --check` | Exit 0 |
| frozen SHA-256 and governed-input diff checks | Exit 0; approved hashes and inputs unchanged |
| JSONL structure/hash check | Exit 0; 19 valid records, 2 M3 records, both human-validation states pending |
| final `git status --short --branch` | Exit 0; clean `codex/m3-builder` worktree |

One exploratory ancestry command contained an unmatched shell glob and emitted
a shell parsing error; it was read-only, changed no files, and the corrected
technical-to-governance and baseline-to-governance ancestry checks both exited
0.

The cumulative test arithmetic is:

```text
M2: 218 = models 72 + package loader 57 + Schema/Python parity 43
             + CAS 18 + Registry 22 + Hypothesis/property 6
M1 formal cases: 30
M0 specification-lock tests: 4
M3 focused integration/security tests: 28
Total: 218 + 30 + 4 + 28 = 280 passed
```

The final full and focused runs reported no `skip`, `xfail`, or `deselected`
tests.

## Claim and Research-Question Boundary

| Item | M3 support now available | Still absent |
|---|---|---|
| C1 | Source-grounded candidate/evidence relation and fail-closed trust transition are executable and linked to M1 traceability obligations | Production replacement correctness, behavioral TER/PIP/BSR, activation, and rollback evidence |
| C2 | Engineering prototype path for deterministic ingestion, source-map binding, quarantine, public loader validation, and M2-backed publication | M4 resolver/compiler, M5 validator/swap controller, M6 adapters, and a full vertical slice |
| C3 | No benchmark task, gold label, or reviewer result created | CapsuleBench tasks, objective truth, licensing, and second-review coverage |
| C4 | No agent comparison or baseline run | TER/PIP/BSR measurements, baseline advantage, cost, and cross-agent replication |
| C5 | New source/test modules and auditable Red-Green/verification records are committed; AI ledger is append-only | Anonymous replication package, raw experiment data, replay, citation/anonymity/page audits |

For RQ1--RQ4, M3 contributes engineering support only to RQ2's planned
provenance/source-span fidelity and to the implementation substrate referenced
by RQ3. No agent task, user study, benchmark, effect size, TER, PIP, BSR,
baseline comparison, or cross-agent generality result was generated. Unit,
integration, security, and formal tests are not empirical study outcomes.

The related-work boundary remains unchanged: ContractCapsule is not summary
plus metadata, compression-only, retrieval-only/RAG, memory-only, or atom-only;
typed atoms, provenance, schemas, CAS, Skills, MCP, and adapters are not
standalone novelty claims. Context Codec remains a high-overlap adjacent work.

## Limitations, Deviations, and Pending Decisions

- The quarantine store is process-local and append-only for this module. Durable
  multi-process promotion records, revocation, role inheritance, and
  cross-tenant authorization are out of scope.
- The HMAC approval authority is an injected test/research adapter, not a
  production identity, signature, or institutional human-review service.
- Git and external immutable evidence are validated structurally/offline; M3
  does not fetch remote objects or prove remote availability.
- Source checks assume a stable local filesystem snapshot; a hostile concurrent
  process racing path resolution is not claimed to be contained.
- Secret detection is a high-confidence heuristic gate, not a complete secret
  discovery or legal data-classification decision.
- M3 does not implement eligibility, ranking, mandatory dependency closure,
  conflict resolution, view compilation, activation, rollback, adapters, or
  benchmark experiments.
- Human validation of this module and its trust policy remains pending. Codex
  is not an author, and no author identity or detailed conflict information is
  written to tracked research files.

## Frozen Baseline Checks

- CCS-2.1 SHA-256:
  `aaddaa8def8df1ef2efbe4f9487de0ba86a60ae5aed8955ca76f5e070fa01e5c`
- Frozen execution plan SHA-256:
  `7c1feebb36776c09de5c0ba5462540072374b9902d8e04d405585a8efb8659c0`
- Both hashes matched their approved values. Neither frozen file and no
  `research/protocol.md` content was modified.
- AI usage records are in `research/ai-usage-ledger.jsonl`; this module adds
  records for implementation/testing and governance reporting. Human approval
  is not recorded as complete by this report.

## M3 Exit Gate

The sole M3 exit condition is satisfied by the implementation and focused
security/integration evidence: a generated candidate cannot enter a published
P0/P1 payload without exact evidence binding and a trusted, recorded human
approval. The engineering result is **complete pending author review**, not an
author-approved research result.

M3 Builder, source-map, trust classification, and quarantine work is complete;
M4 has not started and requires a separate author authorization.
