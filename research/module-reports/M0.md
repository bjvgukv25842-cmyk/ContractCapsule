# M0 Module Report

## Status and Scope

- Authorized module: M0 - Research governance, repository bootstrap, and paper
  charter.
- Technical status: implementation and verification complete; the Git identity
  blocker is resolved and the local M0 technical and audit commits succeeded.
- Governance status: the author approved the M0 technical outputs and human
  exit gate on 2026-08-01. M0 is closed.
- Execution driver: manual frozen-checklist execution explicitly authorized by
  the human author. `superpowers:executing-plans` was not available, installed,
  loaded, or claimed as used.
- Scope boundary: no M1-M11 implementation, experiment, ContractCapsule Skill,
  MCP server, agent adapter, UI, cloud service, or marketplace work started.

## Environment

- Repository root: `/Users/litmus/Documents/Contract Capsule`.
- Branch: `codex/m0-governance`.
- uv: 0.12.1 (`329541a50`, 2026-07-31, aarch64-apple-darwin).
- Project Python: CPython 3.12.13 at `.venv/bin/python3`.
- uv-managed Python used to initialize the project:
  `/Users/litmus/.local/share/uv/python/cpython-3.12-macos-aarch64-none/bin/python3.12`.

Absolute local paths are retained here for M0 auditability and must be removed
or normalized during the M11 anonymity audit.

## Created and Modified Files

- Retained without modification: `AGENTS.md`,
  `contract-capsule-v2.1-frozen-spec-cn.md`, and
  `2026-07-30-contract-capsule-fse-2027-execution-plan.md`.
- Created by uv/bootstrap: `.python-version`, `pyproject.toml`, `uv.lock`,
  `src/contractcapsule/__init__.py`, and `src/contractcapsule/py.typed`.
- Created for repository hygiene and orientation: `.gitignore`, `README.md`.
- Byte-copied frozen baselines: `docs/spec/CCS-2.1.md` and
  `docs/superpowers/plans/2026-07-30-contract-capsule-fse-2027-execution-plan.md`.
- Created specification lock: `src/contractcapsule/spec_lock.py`.
- Created tests: `tests/unit/test_spec_lock.py`.
- Created governance records: `research/claim-evidence-matrix.md`,
  `research/protocol.md`, `research/ai-usage-ledger.jsonl`, and
  `research/decision-log.md`.
- Created anonymous paper skeleton: `paper/main.tex`.
- Created this report: `research/module-reports/M0.md`.
- Updated local Git configuration: `origin` points to the author-approved
  GitHub HTTPS repository; `.git/config` is not a tracked project artifact.
- Ignored but not deleted: `.DS_Store`, `.venv/`, Python bytecode, pytest,
  Ruff, mypy, coverage, and build caches.

## Commands and Actual Results

| Command | Exit/result |
|---|---|
| `command -v uv` | Exit 0; `/Users/litmus/.local/bin/uv` |
| `uv --version` | Exit 0; uv 0.12.1 |
| `uv python find 3.12` | Exit 0; uv-managed CPython 3.12 path found |
| `git status --short` | Exit 0; four initial untracked items, including ignored-later `.DS_Store` |
| `git rev-parse --show-toplevel` | Exit 0; current directory confirmed as repository root |
| Source `shasum -a 256` verification | Exit 0; both candidate digests matched |
| `git switch -c codex/m0-governance` | Exit 0; branch created |
| `uv init --python 3.12 --lib --name contractcapsule --vcs none --author-from none .` | Exit 0; existing repository preserved, CPython 3.12.13 selected |
| `uv add pydantic typer networkx pyyaml jsonschema` | Exit 0; five approved direct runtime dependencies added |
| `uv add --dev pytest pytest-cov hypothesis ruff mypy` | Exit 0; five approved direct development dependencies added; uv emitted one dependency-metadata version-specifier repair warning |
| Source-to-target `cmp -s` checks | Exit 0; both copies byte-identical |
| First `uv run pytest tests/unit/test_spec_lock.py -v` (RED) | Non-zero; collection failed with `ModuleNotFoundError: contractcapsule.spec_lock` as intended |
| Second specification-lock pytest run (GREEN) | Exit 0; 4 tests passed |
| First full `uv run ruff check .` | Exit 1; two `I001` import-order findings retained as a real failure |
| `uv run ruff check src/contractcapsule/spec_lock.py tests/unit/test_spec_lock.py --fix` | Exit 0; 2 findings fixed, 0 remaining |
| Final `uv run pytest tests/unit/test_spec_lock.py -v` | Exit 0; 4 tests passed in 0.08 seconds |
| Final `uv run ruff check .` | Exit 0; all checks passed |
| `uv lock --check` | Exit 0; 35 total packages resolved from the lock |
| AI-ledger JSONL parse check | Exit 0; 2 records parsed |
| Final baseline `cmp` plus `sha256sum --check` | Exit 0; both target baselines reported `OK` |
| Test inventory | Exit 0; `tests/unit/test_spec_lock.py` is the complete current suite |
| Paper/protocol static requirement scan | Exit 0; anonymous class, Data Availability, page, review, artifact, and AI-use requirements found |
| Guarded Git identity check | Exit 1; both `user.name` and `user.email` are missing |
| Read-only identity and ignore follow-up | Exit 0; both fields confirmed missing, `.DS_Store` confirmed ignored |
| `git ls-remote https://github.com/bjvgukv25842-cmyk/ContractCapsule.git HEAD` | Exit 0; no HEAD returned, consistent with an empty remote |
| `git remote add origin https://github.com/bjvgukv25842-cmyk/ContractCapsule.git` | Exit 0; fetch and push URLs verified; no push performed |
| Repository-local Git identity configuration | Exit 0; public GitHub profile name and privacy-preserving noreply email read back successfully |
| `git commit -m "chore: bootstrap ContractCapsule FSE research project"` | Exit 0; root commit `52a5c2da76143a53eb8a554b6550eee5d9dfffcf` created with 20 reviewed files |
| M0-close repository and commit preflight | Exit 0; clean `codex/m0-governance` worktree and both `52a5c2d` and `e7fab32` verified as commits |
| M0-close source/target digest and byte-copy checks | Exit 0; both source and normative copies match their approved SHA-256 values and each other |
| M0-close `uv run pytest tests/unit/test_spec_lock.py -v` | Exit 0; 4 tests passed in 0.09 seconds |
| M0-close `uv run pytest -v` | Exit 0; complete current suite of 4 tests passed in 0.09 seconds |
| M0-close `uv run ruff check .` | Exit 0; all checks passed |
| M0-close `uv lock --check` | Exit 0; 35 packages resolved from the lock |
| M0-close AI-ledger JSONL parse | Exit 0; 6 records parsed |
| M0-close tracked-file identity scan | Exit 0; supplied author identity details are absent from tracked workspace files |

No valid unfavorable test or lint result was discarded. The RED test and first
Ruff failure are retained above; later passing runs followed actual changes.

## Frozen Baseline Verification

- CCS-2.1:
  `aaddaa8def8df1ef2efbe4f9487de0ba86a60ae5aed8955ca76f5e070fa01e5c`.
- Execution plan:
  `7c1feebb36776c09de5c0ba5462540072374b9902d8e04d405585a8efb8659c0`.
- Both root sources and normative copies match.
- Neither frozen baseline was reformatted, translated, checked off, or edited.
- Fault injection appends bytes to a temporary copy and confirms activation of
  the lock fails closed with `FrozenBaselineMismatch`.

## Contribution and Research-Question Support

| ID | M0 support | Evidence not yet produced |
|---|---|---|
| C1 | Digest-locked CCS-2.1 foundation | M1 formal model and replacement relation |
| C2 | Python 3.12 project and baseline guard only | M2-M6 working prototype and vertical slice |
| C3 | Frozen benchmark governance retained | M7 tasks, gold review, and scorers |
| C4 | Frozen conditions and measures retained | M8-M10 comparative runs and analysis |
| C5 | Environment lock, protocol, ledger, decision log, and paper skeleton | Anonymous reproducibility artifact and clean replay |
| RQ1 | Identifier and measures recorded | No effectiveness or efficiency result |
| RQ2 | Traceability requirements recorded | No provenance measurement result |
| RQ3 | TER, PIP, and BSR separation preserved | No replacement-safety result |
| RQ4 | Cross-agent and ablation plan preserved | No generality or ablation result |

M0 creates governance and traceability foundations. It produces no benchmark,
agent run, statistical result, verified citation, or evidence for a favorable
paper conclusion.

## AI Use

- Ledger: `research/ai-usage-ledger.jsonl`.
- Recorded roles: implementation and writing.
- Codex assisted with project interpretation, repository bootstrap, dependency
  setup, specification-lock code and tests, governance records, and the paper
  skeleton.
- Codex is not listed as an author.
- Exact model ID and original prompt SHA-256 were not exposed by session
  metadata. They are marked unavailable rather than fabricated.
- Human validation of the M0 technical outputs and exit gate is approved.

## Deviations, Risks, and Pending Decisions

- Approved execution-driver deviation: M0 used the frozen checklist manually
  because the required Superpowers Skill was not registered.
- Existing-repository adaptation: `uv init` used `--vcs none`, and no nested
  repository was created.
- Anonymous-author adaptation: `--author-from none` prevented inferred author
  metadata.
- The local-path audit record is an anonymity risk to remove at M11.
- `LICENSE` and `CITATION.cff` were not created because license selection,
  authors, and affiliations were not supplied; inventing them would violate
  research integrity.
- M1-M11 files from the full Section 6 map were not pre-populated; only the M0
  minimum skeleton was created as explicitly authorized.
- LaTeX compilation was not part of the frozen M0 command list and `latexmk`
  was not available during preflight. The skeleton therefore has not been
  compiled in M0.
- AI run metadata capture is incomplete and must be fixed before experiments.
- The initial Git identity failure was preserved in this report. It was resolved
  only after author authorization, GitHub public-profile verification, and use
  of a privacy-preserving noreply email. Paper authorship was separately
  confirmed by the author; identity details remain outside tracked public files.
- The author reports no known submission conflicts.
- The current protocol has no human-participant data. Adding user studies,
  interviews, surveys, private developer data, crowdsourced labeling, or
  identifiable telemetry reopens the ethics gate and requires institutional
  review.
- Repository license and citation metadata remain a later author decision; they
  are not part of the frozen M0 exit criteria.
- M1 preflight is blocked because `superpowers:executing-plans` is not
  registered in this session. The M0 manual waiver does not apply to M1.

## M0 Human Exit Gate

| Gate item | Status |
|---|---|
| Target track: FSE 2027 Research Papers | Confirmed |
| Exact CCS-2.1 digest | Confirmed |
| Exact frozen execution-plan digest | Confirmed |
| Final author names and affiliations | Confirmed; details stored outside tracked public files |
| Submission conflicts of interest | Confirmed; author reports no known conflicts |
| Human-participant or other ethics review requirement | Confirmed for current non-human-participant protocol |
| Repository license and citation metadata | Later author decision; not an M0 exit criterion |
| Technical tests and lint | Passed |
| Local M0 commit | Completed: `52a5c2da76143a53eb8a554b6550eee5d9dfffcf` |
| Local M0 audit commit | Completed: `e7fab322939ef3a034bb686caa07a0c1451c5dae` |
| Human M0 exit approval | Approved, 2026-08-01 |

M0 technical artifacts, verification, and human governance are complete. At
the time M0 closed, M1 had not started because its required execution Skill was
unavailable; M2 remained unauthorized.

## Post-M0 Gate Note - 2026-08-01

The author-approved M0 gate remains closed. In a later session, the official
curated Superpowers plugin registered `superpowers:executing-plans`; the Skill
and its required supporting instructions were read completely. The historical
M0 manual-execution waiver was not extended. M1 then began on isolated branch
`codex/m1-formal-model`. This note records the resolved preflight status and
does not alter M0's frozen commands, evidence, or execution driver.
