# M5-4B 回归补充计划（待逐项批准）

技术基线：`6bdc657fa8305b6b7fa6bd38d0d30fe0f3496043`。独立审查包以
`M5-4B-review.diff.gz` 无损保存；解压后仍是审查时的2309行 unified diff。
依据：`M5-4B-independent-review.md` 的两项 Important 发现，结论为
REQUEST CHANGES。当前未修改受审源码或测试，也未开始 M5-5。

## 已完成及验证边界

- 已批准的 Docker image/inspect 畸形响应及深层 JSON 修复，在 `d5ad681`
  取得独立 scoped PASS，原失败报告和回归保留。
- M5-4B 已有真实低风险单对、高风险三对，以及前置失败、撤权、重放、重启、
  来源对象篡改和持久化失败的工程测试。
- 本次主控完整回归：`uv run --locked --offline pytest -q`，
  **1007 passed in 211.11s**；Task4 聚焦集 **69 passed in 150.80s**。
- No-ignore Ruff 默认/四项复杂度、Mypy（67 个实际源文件）、离线锁文件和
  两份冻结文件/ADR 摘要通过。早期 `git_tree` 复杂度失败已由 6bdc657 收口。
- 这些是实现验证。独立审查没有重跑上述全量测试，也尚未给 Task4B PASS。
  原报告把低层 stage 资源测试概括成 twin 层证据的表述，以这里和独立发现为准。

## 批准后执行的具体改动

| 编号 | 范围 | 验收断言 |
|---|---|---|
| M5-4B-R1 | `tests/unit/test_twin_orchestration.py` 增加真实 Git replacement-ref 回归 | 临时仓库创建两个内容不同的提交，并建立指向替代提交的 `refs/replace/<locked>`；直接 Git 对照证明替代引用有效，`git_tree` 仍读取锁定原提交的准确字节和摘要 |
| M5-4B-R2 | `tests/integration/test_twin_pair.py` 增加真实 Docker 超时与输出超限两类集成回归，必要时窄范围扩展 `tests/m5_twin_helpers.py` | 使用发布前已锁定的测试程序及较小的已支持限制，确认失败不会产生有效 TwinOutcome；认证失败清单/进程限额标志和真实终止状态可读，同一 attempt 重放不创建新执行，也不将未知进程状态认证为完成 |

默认仅补测试及证据，不改生产接口、核心 Schema、依赖、限制上限或行为语义。
若新测试暴露需要生产修改的不同问题，先保存失败及独立判断，不将本次批准
扩大为无界修复授权。当前模块尚无活动指针，不能伪造“指针未变”的断言；
该断言将由原计划 M5-6/M5-7 的真实持久化状态测试完成。

## 验证与继续顺序

1. R1/R2 在临时仓库及真实受限 Docker 中执行；对失效实现做独立副本故障注入，
   由目标行为断言捕获，不把导入、夹具或环境错误算作有效 RED。
2. 聚焦用例通过后执行累计回归、Ruff/Mypy、普通和 no-ignore 四项复杂度、
   实际扫描清单、锁文件、冻结摘要及 `git diff --check`；保留原始失败证据。
3. 仅提交获批的测试/夹具文件，交给原独立审阅者复核两项发现及改动带来的问题。
   更新决策日志、证据矩阵、AI 台账和 M5 报告。
4. Task4B 独立通过后，按已有授权继续 M5-5 状态/安全边界、M5-6 原子激活/回滚、
   M5-7 完整审计与归档，最后停在 M5 作者退出门。

补测与本地验证预计 0.5 个工程日，独立复核等待另计；不是新的日程承诺。
此次暂停来自已批准 M5 计划第214行：
`New blocking findings require itemized repair approval.`
需要作者批准 **M5-4B-R1、M5-4B-R2** 后执行。

**M5 未完成；正式进度仍为 M0-M4：5/12（41.7%）。未开始 M6，未开展正式实验。**
