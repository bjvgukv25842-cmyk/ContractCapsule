# M2 Module Report

## Status and Authorization

- Authorized module: M2 -- Canonical schemas, package loader, CAS, and
  immutable Registry.
- Technical status: **M2 REMEDIATION ACCEPTED; M2 EXIT GATE FORMALLY
  APPROVED** by the author on 2026-08-03 at approved implementation HEAD
  `eec2c1fd173748f181dc8b672b3be320035426a8`.
- M0 gate: approved and closed. G0/M1: approved with bounded novelty scope and
  locally integrated before this branch was created.
- Input baseline branch: `codex/m0-governance`.
- Input baseline commit:
  `c24696e88117b70ed386d586ce9d548951f6287d`.
- M2 branch: `codex/m2-canonical-storage`.
- Isolated worktree:
  `/Users/litmus/Documents/Contract Capsule/.worktrees/m2-canonical-storage`.
- M2 implementation commit:
  `fac9522a4abd7da85a794b5fcbf59ec012259b8b`.
- M2 remediation audit starting HEAD:
  `78eb719f6ca5dd9b97eb964e6f54a17aa34e028c`.
- M2 remediation implementation commit:
  `3b3cb870a05c1af7458268f66da37a9fc5e9474f`.
- Scope boundary: no Builder, Resolver, ranking, View Compiler, behavior
  dual-run, active-version pointer, activation, rollback, MCP, Skill, adapter,
  benchmark run, UI, push, merge, or PR was implemented.

## Schedule Deviation

M2 began on 2026-08-02, six days before the frozen 2026-08-08 through
2026-08-12 window. The author explicitly approved this early start after M1
passed G0 and was integrated. No module order, TDD, integrity, research,
verification, or human-exit requirement was waived.

The first M2 preflight stopped before branch creation or any write because four
safety-critical semantics were not uniquely determined. The author then froze
`CCS-2.1-canonical-v1`, CAS authorization, exact-republish behavior, and the
three Evidence modes. The stop and resolution are recorded as M2-001 and
M2-002 in `research/decision-log.md`.

## Skills and Execution Boundary

The registered curated Skills were read completely and used:

- `superpowers:executing-plans`;
- `superpowers:using-git-worktrees`;
- `superpowers:test-driven-development`;
- `superpowers:verification-before-completion`.

Independent read-only reviews covered the canonical model, package loader,
and CAS/Registry. Their concrete fail-open findings were converted into tests
before fixes. No unavailable Skill was claimed and no plugin code was
installed.

## Files Created

- `src/contractcapsule/models/base.py`
- `src/contractcapsule/models/core.py`
- `src/contractcapsule/models/canonical.py`
- `src/contractcapsule/models/schema.py`
- `src/contractcapsule/models/__init__.py`
- `src/contractcapsule/package.py`
- `src/contractcapsule/storage/cas.py`
- `src/contractcapsule/storage/registry.py`
- `src/contractcapsule/storage/__init__.py`
- `schemas/capsule.schema.json`
- `schemas/control-manifest.schema.json`
- `schemas/semantic-payload.schema.json`
- `schemas/evidence-plane.schema.json`
- `schemas/dependency-graph.schema.json`
- `schemas/replacement-contract.schema.json`
- `schemas/compression-policy.schema.json`
- `schemas/tests-integrity.schema.json`
- `tests/m2_helpers.py`
- `tests/unit/test_models.py`
- `tests/unit/test_package_loader.py`
- `tests/unit/test_cas.py`
- `tests/unit/test_registry.py`
- `tests/property/test_immutability.py`
- package marker files under `tests/`
- `research/module-reports/M2.md`

## Files Modified

- `pyproject.toml`
- `uv.lock`
- `research/claim-evidence-matrix.md`
- `research/decision-log.md`
- `research/ai-usage-ledger.jsonl`

The frozen CCS-2.1, frozen execution plan, and frozen
`research/protocol.md` were not modified.

## Seven-Module Model and Schema Coverage

| CCS-2.1 core module | Python model | Static Schema | Package carrier |
|---|---|---|---|
| Control Manifest | `ControlManifest` | `control-manifest.schema.json` | `manifest.json` |
| Semantic Payload | `SemanticPayload` and ordered `Atom` records | `semantic-payload.schema.json` | `payload/atoms.jsonl`; optional extension metadata |
| Evidence Plane | `EvidencePlane` and discriminated Evidence records | `evidence-plane.schema.json` | `evidence/source-map.jsonl`; optional extension metadata; optional Fat Export blobs |
| Dependency Graph | `DependencyGraph` and `DependencyEdge` | `dependency-graph.schema.json` | `graph/dependencies.json` |
| Replacement Contract | `ReplacementContract` | `replacement-contract.schema.json` | `contracts/replacement.yaml` |
| Compression Policy | `CompressionPolicy` with P0-P4 exactly once | `compression-policy.schema.json` | `policies/compression.yaml` |
| Tests & Integrity | `TestsIntegrity`, lock, checksums, signature policy | `tests-integrity.schema.json` | test artifacts plus `integrity/capsule.lock`, `checksums.sha256`, and declared detached signature |

All models reject unknown fields, coercive input, non-finite values, malformed
digests/versions/dates, unsafe security paths, invalid modes, unresolved or
non-reciprocal Evidence bindings, duplicate identities, and inconsistent
signature metadata. Tuples and recursively frozen mappings prevent in-place
mutation. Registry publication re-enters the strict validation boundary so a
Pydantic `model_copy(update=...)` cannot bypass publication invariants.

The eight static documents are generated deterministically from the model
definitions and compared byte-semantically in tests. The authoritative
`validate_capsule_document` entry point applies Draft 2020-12 structure first
and the mandatory Python semantic pass second. Generic JSON Schema engines
cannot express every cross-record identity/reference relation; the root Schema
declares the semantic entry point with
`x-contractcapsule-semantic-validation`. This limitation is not hidden or
reported as raw-Schema equivalence.

## CCS-2.1-canonical-v1 Identity Projection

The implementation builds an explicit whitelist projection. It does not dump
the whole Capsule and remove fields afterward.

| Module | Included paths |
|---|---|
| Projection root | `profile` fixed to `CCS-2.1-canonical-v1` |
| Control Manifest | spec/profile, ID/version, owner/tenant, all scope arrays, authority, sensitivity, provides/requires/conflicts, lifecycle, created-from, integrity lock/signature references, extensions |
| Semantic Payload | module extensions; every ordered Atom field including validity, authority, status, confidence, evidence/dependency/conflict references, sensitivity, compression class, refresh policy, extensions |
| Evidence Plane | module extensions; every ordered common Evidence field including digest, media type, captured time, retention, access policy and validation; complete Git locator fields; complete external source/verification fields |
| Dependency Graph | module extensions and every ordered edge field, version constraint, mandatory bit, and edge extensions |
| Replacement Contract | identity/replaces, preconditions, effects, protected invariants, allowed/forbidden scope, all verification commands, activation policy, rollback policy, extensions |
| Compression Policy | version, ordered P0-P4 class fields and expansion/TTL rules, extensions |
| Tests & Integrity | ordered tests and checksums, complete dependency/source/compiler/adapter/policy/test/model/parameter/runtime lock fields, signature policy, extensions |

Exact included leaf paths are enumerated in M2-003 of
`research/decision-log.md`. Explicit exclusions are:

- `control_manifest.content_digest` and any other repeated root digest;
- detached-signature transport fields, including value and envelope; normative
  signature algorithm/key/profile remain identity-bound in signature policy;
- package-byte digest, which M2 does not expose;
- `derived_artifacts.*`, including Embeddings, cache, indexes, and Compiled
  Views;
- `runtime_sidecar.*`, including logs, observations, and runtime timestamps.

JCS/RFC 8785 serialization uses UTF-8, recursive key ordering, no insignificant
whitespace, and no Unicode normalization. Array order is retained and is
identity-relevant under the author's canonical-v1 rule. Non-string object
keys, lone surrogates, duplicate input keys, unsafe integers, NaN, and
infinities fail closed. The result format is exactly `sha256:` plus 64
lowercase hexadecimal characters. Future signing input is frozen as
`b"CCS-2.1-signature-v1\0" + content_digest.encode("ascii")`; no signing or
production signature-security result is claimed.

M1's `repr()` digest remains a decision witness only. M1's mathematical set
notation represents semantic membership; canonical-v1 uses the author-approved
ordered transport representation because CCS-2.1 does not freeze a unique sort
key for these arrays.

## Package Loader Trust Boundary

Required frozen carriers are parsed with strict UTF-8, duplicate-key rejection,
finite/exact numeric checks, and safe YAML restrictions. The two optional
metadata carriers hold only module-level extension objects; they preserve
canonical-v1 extensions without becoming new core modules and may be absent
when empty.

The loader rejects:

- missing core carriers or safety-critical fields;
- unknown carrier keys, files, modes, or cross-mode fields;
- duplicate logical/case-folded files and duplicate JSON/YAML keys;
- YAML aliases, anchors, tags, and non-JSON values;
- invalid UTF-8, lone surrogates, NaN, Infinity, and unsafe integers;
- absolute, drive-qualified, non-literal, traversal, or backslash paths;
- root, directory, file, or bundled-blob symlinks and non-regular entries;
- incomplete or contradictory Atom/Evidence references;
- missing or mismatched test artifacts and CAS Evidence bytes;
- malformed Git commit OIDs or unsafe repository-relative paths;
- noncanonical HTTPS/persistent identifiers;
- declared/core digest mismatch.

`CAS` records require locally bundled and rehashed bytes. `GIT_IMMUTABLE`
records require canonical repository identity, algorithm-tagged full OID,
literal relative path, locator digest, Evidence digest, and media type.
`EXTERNAL_IMMUTABLE` records require a canonical HTTPS/DOI/URN locator,
Evidence digest, media type, source, and verification method. The latter two
modes perform no network fetch and never fall back to CAS.

No partial Capsule is returned and loading performs no persistent write.
Callers must supply a stable local package snapshot: M2 rejects static
symlinks and traversal but does not claim containment of a separate hostile
process concurrently replacing directory entries during a load.

## Filesystem CAS Semantics

- Payload SHA-256 selects `objects/sha256/<2>/<62>.blob`.
- A single canonical envelope stores digest, media type, size, and bytes.
- Temporary and final entries share a filesystem directory.
- The complete temporary envelope is flushed and file-synced before
  no-replace hard-link publication; the containing directory is then synced.
- Per-digest advisory locks serialize reads and writes across processes, so a
  failed post-link durability check cannot be observed as a successful object
  by a concurrent operation.
- Existing objects are fully rehashed and their media type is compared; no
  public overwrite or deletion operation exists.
- Static symlinked internal directories and digest prefixes fail closed.
- Writes grant no read access. Direct reads default deny and require an exact
  trusted Boolean authorization result.

The plan's shorthand “atomic rename” was realized with an atomic no-replace
hard link because ordinary replace/rename semantics could overwrite an
existing immutable object. This is a safety-preserving implementation choice,
not a weakening of the plan.

## SQLite Registry Semantics

| Table | Key/invariant | Stored facts |
|---|---|---|
| `publications` | unique `(capsule_id, version)` | digest, lifecycle, authority, scope JCS, immutable core JCS, detached transport record, original timestamp |
| `evidence_references` | primary `(publication_id, evidence_id)` and unique ordinal | mode, digest, media type, access policy, immutable Evidence JCS |
| `digest_grants` | primary `(publication_id, evidence_id, digest)` and foreign key to Evidence | published-reference read grant constraint |

`publish()` strictly revalidates the candidate, validates PUBLISHED lifecycle
without mutating it, resolves publish permission, starts `BEGIN IMMEDIATE`,
then privately rehashes referenced CAS bytes and media type before inserting
publication, Evidence, and grants in one SQLite transaction. Any failure rolls
back Registry state. A successful earlier CAS write may remain as an unreadable
orphan because SQLite and the filesystem cannot form one transaction.

Trusted Policy Resolver decisions are test-injectable and default deny.
Malformed/truthy results deny. Effective permission is Resolver permission
intersected with Capsule access policies; those declarations can only narrow.
`get_blob()` uses the union of independently valid published references and
revalidates the full publication, Evidence row, grant, CAS digest, CAS media
type, and bytes before returning data.

Exact republish reauthorizes and compares every immutable publication,
signature, Evidence, and grant field. It returns the original row and
timestamp without a write. Same ID/version with another digest raises a
version conflict; another immutable difference raises a publication conflict.
There is no `INSERT OR REPLACE`, `REPLACE INTO`, update, delete, activity
pointer, activation, or rollback API.

## Relationship to M1 Formal Semantics

| M1 obligation | M2 executable foundation | Not yet implemented |
|---|---|---|
| Capsule well-formedness | seven strict modules, closure and reciprocal Evidence validation | M3 source trust/promotion |
| Evidence preservation | immutable locators/digests, bundled-CAS verification | M3 ingestion and real source snapshots |
| Eligibility before ranking | authority, scope, lifecycle, access policy persisted | M4 Resolver/ranking pipeline |
| P0 preservation | P0-P4 policy and exact P0 rule modeled | M4 budgeted view compilation |
| Mandatory closure/conflict visibility | dependencies and conflicts identity-bound | M4 graph resolution |
| Interface/replacement correctness | replacement contract identity-bound | M5 behavior runner/decision |
| Atomic activation/safe rollback | immutable old/new version substrate only | M5 active pointer transaction |
| Irreversible-side-effect boundary | activation/rollback policy stored | M5 enforcement and compensation checks |

M1's 30 formal tests remain unchanged and pass. M2 does not claim to execute
replacement behavior.

## Red-to-Green Evidence

| Stage | Command/result | Representative evidence |
|---|---|---|
| Initial Red | M2 module command, Exit 2 | collection failed because `contractcapsule.models` did not exist |
| Model Red | model suite, Exit 1 with 10 failures | missing modules, mutability, identity, JCS, and Schema properties were executable before implementation |
| Loader Red | loader suite, Exit 1 with 18 failures and 6 passes | malformed packages, digest mismatch, traversal, missing blobs, and unknown files were not yet enforced |
| Review Red | targeted model/loader/CAS/Registry commands, nonzero | exposed model-copy bypass, malformed carrier keys, reciprocal Evidence mismatch, literal-path gaps, malformed policy decisions, CAS media tamper, symlink redirects, and post-link concurrency visibility |
| Final adversarial Red | 23 targeted tests, Exit 1; 23 failed | all newly reviewed fail-open paths were reproduced before fixes |
| Green | M2 module command, Exit 0; 172 passed | strict model/Schema boundary, loader, CAS, Registry, and property cases pass together |

No negative test was deleted, skipped, marked xfail, or weakened. The CAS
post-link race, truthy policy result, metadata-carrier drop, and Evidence
contradiction were retained as regressions.

## Dependencies

- Added runtime dependency `rfc8785` 0.1.4, constrained to `<0.2`, because
  Python's ordinary JSON serializer does not implement RFC 8785/JCS number and
  UTF-16 key-order rules.
- Added development stubs `types-jsonschema` and `types-pyyaml` so the existing
  strict mypy check covers Schema and YAML loader code.
- `uv.lock` changed only for these additions and resolves 38 packages.
- Hypothesis was already present; it was not newly added or used to upgrade an
  unrelated package.

## Contribution and RQ Evidence Boundary

| Claim/RQ | M2 support | Evidence still absent |
|---|---|---|
| C1 | production field model is consistent with the M1 seven-part relation and keeps Evidence/contract/dependency semantics explicit | independent formal proof and empirical behavior |
| C2 | engineering evidence for canonical identity, safe loading, local immutable CAS, and Registry | M3-M6 end-to-end prototype and G1 vertical slice |
| C3 | no benchmark artifact produced | all 24 tasks, licensed repositories, objective truth, and human review |
| C4 | no experiment or baseline run produced | every TER/PIP/BSR observation, effect, interval, p-value, and cross-agent result |
| C5 | reproducible lock, generated Schema set, tests, decisions, AI ledger, and module report | anonymous release package, clean external replay, raw data, final audits |
| RQ1 | no effectiveness/efficiency result | all agent task and token/cost outcomes |
| RQ2 | structural and immutable provenance mechanisms are executable | real source-map precision, span fidelity, expansion, and user audit |
| RQ3 | immutable version substrate and contract fields are executable | behavior dual-run, TER/PIP/BSR, activation, rollback |
| RQ4 | Agent-independent storage interface only | adapters, replication, ablations, generality |

Unit, property, and formal-case tests are engineering evidence, not benchmark
experiments. C2-C5 are not empirically established. Context Codec remains a
high-overlap neighbor. Typed atoms, provenance, canonical identity,
compression verification, RAG, Skill, MCP, CAS, and Registry are not claimed
as standalone novelty.

## Verification Results

| Command | Exit/result |
|---|---|
| `uv run pytest tests/unit/test_models.py tests/unit/test_package_loader.py tests/unit/test_cas.py tests/unit/test_registry.py tests/property/test_immutability.py -v` | Exit 0; 172 passed |
| `uv run pytest tests/fixtures/formal_cases -v` | Exit 0; 30 passed |
| `uv run pytest -v` | Exit 0; 206 passed |
| `uv run ruff check .` | Exit 0; all checks passed |
| `uv run mypy .` | Exit 0; no issues in 23 source files |
| `uv lock --check` | Exit 0; 38 packages resolved |
| `git diff --check` | Exit 0 |
| `uv run ruff check src/contractcapsule tests --select C901,PLR0911,PLR0912,PLR0915` | Exit 0 |
| Targeted static-Schema regeneration and semantic-consistency tests | Exit 0; 2 passed |
| First literature/JSONL one-line parser commands | Exit 1; two shell/Python quoting `SyntaxError` results retained; no data changed |
| Corrected literature structure parser | Exit 0; 11 rows and 11 verified citations |
| Corrected AI-ledger JSONL parser | Exit 0; 14 valid records, including 2 M2 records before the commit audit entry |
| Frozen source/target `cmp` checks | Exit 0 for both byte-identical pairs |
| Frozen target SHA-256 check | Exit 0; both approved values match |
| Frozen-baseline/protocol diff against M2 base | Exit 0; no changes |
| Tracked database/cache/temp-artifact inventory | Exit 0; no tracked matches |
| Final pre-commit branch/worktree check | Exit 0; expected isolated M2 branch at verified base plus reviewed changes |

The full suite includes 30 M1 formal tests and 4 M0 frozen-lock tests. All
outputs above came from the Python 3.12.13 uv environment. The parsing-command
quoting failures are recorded as real failed commands rather than hidden.

## Known Limits, Deviations, and Risks

- Early start is an author-approved six-day schedule deviation.
- Static JSON Schema is structural; mandatory Python semantic validation is
  required for cross-record constraints and is explicitly declared.
- Optional extension metadata carriers are a canonical-v1 transport mapping,
  not extra core modules. Frozen-structure packages with empty module
  extensions do not require them.
- Package loading assumes a stable local snapshot against concurrent directory
  replacement; static symlinks and traversal are rejected.
- Advisory digest locks and no-replace links target POSIX local filesystems;
  distributed/network filesystem semantics are not claimed.
- SQLite and filesystem CAS do not provide one cross-resource transaction;
  unreadable orphan blobs are permitted and no garbage collector is in M2.
- One repository/authority trust domain only; no cross-tenant isolation,
  revocation, dynamic role inheritance, distributed authorization, or
  Registry consensus.
- Signature structure and future signing input are frozen, but M2 performs no
  cryptographic verification and makes no production signature-security claim.
- No package-byte digest is exposed because the container byte format is not
  frozen.
- Registry intentionally drops B/C sidecars on publication; they remain
  reconstructible/non-factual and do not affect identity.
- M1's set notation is adapted to the author's ordered-array canonical-v1
  decision; no M1 behavior test changed.

## AI Use

- Append-only ledger: `research/ai-usage-ledger.jsonl`.
- Three original M2 entries record semantic approval,
  implementation/testing/review scope, generated artifacts, commands, commits,
  limitations, and the then-pending human exit. A fourth M2 record covers the
  authorized remediation and its audit evidence; a fifth records the author's
  formal M2 exit approval. The append-only ledger now contains 17 records.
- Codex is not an author. Generated models, tests, mappings, and explanations
  remain engineering artifacts rather than benchmark truth. The author exit
  approval does not convert them into empirical results.

## Frozen Baseline Verification

- CCS-2.1 expected SHA-256:
  `aaddaa8def8df1ef2efbe4f9487de0ba86a60ae5aed8955ca76f5e070fa01e5c`.
- Frozen execution plan expected SHA-256:
  `7c1feebb36776c09de5c0ba5462540072374b9902d8e04d405585a8efb8659c0`.
- Final actual values are recorded in the verification audit below.

## M2 Exit Gate

| Gate item | Status |
|---|---|
| Identical canonical input gives identical digest | Pass in unit/property tests |
| Real identity-projection content change gives another digest | Pass across all seven modules and property cases |
| Derived artifacts, sidecars, and detached signature transport do not affect identity | Pass |
| Invalid package and digest mismatch fail closed | Pass |
| PUBLISHED overwrite is unavailable through public API | Pass |
| CAS tamper, unauthorized read, symlink redirect, and concurrent failure fail closed | Pass |
| Registry conflict, malformed policy, partial transaction, and metadata tamper fail closed | Pass |
| M1 formal cases have no regression | Pass; 30 passed |
| Module/full tests, Ruff, mypy, lock, complexity, structure, and whitespace | Pass after remediation; 218 M2 and 252 cumulative tests. The original 172/206 snapshot remains preserved above as historical evidence |
| Frozen hashes remain approved values | Pass; both exact approved values |
| C2-C5 missing empirical evidence remains explicit | Pass |
| Human M2 exit approval | Approved by the author on 2026-08-03 for implementation HEAD `eec2c1fd173748f181dc8b672b3be320035426a8` |

The author accepted the remediation and formally closed the M2 exit gate.
This approval does not authorize M3; a separate author instruction is still
required before M3 may begin.

M2 canonical models, package loader, immutable storage, and the authorized
Schema-parity remediation are technically complete, locally verified, and
approved at the M2 human exit gate. M3 has not started.

## M2 Remediation and Audit -- 2026-08-03

### Authorization, Finding, and Scope

- The author audit began from
  `78eb719f6ca5dd9b97eb964e6f54a17aa34e028c` and assigned **M2
  IMPLEMENTATION CONDITIONAL PASS**. It did not formally approve the M2 exit
  gate. The author authorized only the remediation described here; M3 remained
  unauthorized.
- The blocker was a static JSON Schema/Python local-assertion mismatch. The
  original public `load_capsule()` path remained fail-closed; no public loader
  bypass was found or repaired.
- The remediation commit is
  `3b3cb870a05c1af7458268f66da37a9fc5e9474f` (`fix: align M2 schemas
  with model constraints`). Its acceptance-set effect is limited to tightening
  generated Schema assertions so they match constraints already enforced by
  Python. Python validation was not relaxed or broadened.
- Canonical identity, JCS serialization, CAS, Registry, authorization,
  republish behavior, and the `CAS`, `GIT_IMMUTABLE`, and
  `EXTERNAL_IMMUTABLE` Evidence modes did not change. Neither frozen baseline,
  `research/protocol.md`, dependency declarations, nor `uv.lock` changed.

### Authorized Schema Constraints

1. `Atom.scope` now has static `minItems: 1`, matching the existing Python
   non-empty assertion.
2. `ReplacementContract.target_effects` and the already Python-required
   `protected_invariants` now each have static `minItems: 1`.
3. `GIT_IMMUTABLE.path` now carries an ECMA-262-safe exact pattern matching
   the Python safe-relative-path rules. It rejects an empty path, an absolute
   `/` path, a leading drive prefix, any backslash, NUL, empty segments from
   `//` or a trailing `/`, and every literal `.` or `..` segment at either the
   leading or an interior position.
4. `EXTERNAL_IMMUTABLE.uri` now exposes three static lexical families:
   HTTPS with Python `urlsplit` scheme-normalization compatibility, DOI, and
   URN. The HTTPS branch accounts for scheme case, leading C0/space stripping,
   and tab/CR/LF removal performed by `urlsplit`, while still rejecting the
   HTTP family. DOI and URN use explicit, portable Unicode non-whitespace
   classes and an absolute-end guard, including rejection of trailing newline
   text.

The URI Schema remains a lexical precheck, not a replacement for the
authoritative Python semantic pass. Python alone retains canonical HTTPS
host/netloc, credential, port, query, fragment, percent-encoding, trailing or
double slash, and dot-segment checks, as well as the application-level
HTTPS/DOI/URN parser semantics. This disclosed boundary preserves the M2-004
generic-validator limitation rather than claiming raw-Schema equivalence.

All eight Schema artifacts were regenerated deterministically. Four changed:
`capsule.schema.json`, `evidence-plane.schema.json`,
`replacement-contract.schema.json`, and `semantic-payload.schema.json`; the
other four regenerated byte-identically.

### Remediation File Inventory

Commit `3b3cb870a05c1af7458268f66da37a9fc5e9474f` changed exactly eight
tracked files:

- Schema-generation and model metadata:
  `src/contractcapsule/models/base.py` and
  `src/contractcapsule/models/core.py`;
- deterministically generated Schema artifacts:
  `schemas/capsule.schema.json`, `schemas/evidence-plane.schema.json`,
  `schemas/replacement-contract.schema.json`, and
  `schemas/semantic-payload.schema.json`;
- persistent public-loader regressions:
  `tests/unit/test_package_loader.py`;
- new local static Schema/Python parity suite:
  `tests/unit/test_schema_python_parity.py`.

No other implementation, test, Schema, dependency, lock, protocol, or frozen
baseline file changed in the remediation commit.

### Persistent Parser Regression

Persistent raw-byte tests now write each of `NaN`, `Infinity`, and
`-Infinity` into a package carrier and call the public `load_capsule()` entry
point. All three are rejected by the strict parser before model construction,
providing direct regression evidence that the existing public loader remains
fail-closed for non-finite JSON tokens.

### Remediation Red Evidence

| Red stage | Recorded result |
|---|---|
| Initial focused parity run | Exit 1; 13 failed, 16 passed |
| Git newline exact-anchor run | Exit 1; 2 failed, 26 deselected |
| HTTPS normalization run | Exit 1; 3 failed, 28 deselected |
| URI portability run | Exit 1; 4 failed, 39 deselected |
| URI trailing-newline run | Exit 1; 2 failed, 6 passed, 35 deselected |
| Mistyped `-k newline_text` selection | Exit 5; zero tests selected. This was a non-data-changing command error and is retained as such |

These outputs are remediation TDD evidence only. No missing command output or
test result is inferred.

### Fresh Green and Review Evidence

Fresh root verification against
`3b3cb870a05c1af7458268f66da37a9fc5e9474f` produced:

| Command | Exit/result |
|---|---|
| `uv run pytest tests/unit/test_models.py tests/unit/test_package_loader.py tests/unit/test_schema_python_parity.py tests/unit/test_cas.py tests/unit/test_registry.py tests/property/test_immutability.py -q -ra` | Exit 0; 218 passed: models 72, loader 57, Schema parity 43, CAS 18, Registry 22, property 6; no skipped, xfailed, or deselected tests |
| `uv run pytest tests/unit/test_models.py::test_all_static_schema_documents_exactly_match_the_python_models tests/unit/test_models.py::test_authoritative_schema_validation_includes_cross_record_invariants tests/unit/test_schema_python_parity.py -q -ra` | Exit 0; 45 passed; no skipped, xfailed, or deselected tests |
| `uv run pytest tests/fixtures/formal_cases -q -ra` | Exit 0; 30 passed; no skipped, xfailed, or deselected tests |
| `uv run pytest tests/unit/test_spec_lock.py -q -ra` | Exit 0; 4 passed; no skipped, xfailed, or deselected tests |
| `uv run pytest -q -ra` | Exit 0; 252 passed = 218 M2 + 30 M1 + 4 M0; no skipped, xfailed, or deselected tests |
| `uv run ruff check .` | Exit 0; all checks passed |
| `uv run mypy .` | Exit 0; no issues in 24 source files |
| `uv lock --check` | Exit 0; 38 packages resolved |
| `uv run ruff check src tests --select C90,PLR0911,PLR0912,PLR0913,PLR0915` | Exit 0; all checks passed |
| `git diff --check` | Exit 0 |
| `shasum -a 256 docs/spec/CCS-2.1.md docs/superpowers/plans/2026-07-30-contract-capsule-fse-2027-execution-plan.md` | Exit 0; `aaddaa8def8df1ef2efbe4f9487de0ba86a60ae5aed8955ca76f5e070fa01e5c` and `7c1feebb36776c09de5c0ba5462540072374b9902d8e04d405585a8efb8659c0` |

Independent specification and code-quality reviews returned approved after two
review/fix loops. These AI-assisted reviews are engineering evidence only and
do not constitute author approval of the M2 exit gate.

### Frozen Baselines, Evidence Boundary, and Pending Gate

- CCS-2.1 SHA-256 remains
  `aaddaa8def8df1ef2efbe4f9487de0ba86a60ae5aed8955ca76f5e070fa01e5c`.
- Frozen execution-plan SHA-256 remains
  `7c1feebb36776c09de5c0ba5462540072374b9902d8e04d405585a8efb8659c0`.
- C2-C5 empirical evidence remains absent. No TER, PIP, or BSR observation,
  baseline advantage, or cross-agent generality result exists. Context Codec
  remains a high-overlap neighboring work.
- Historical note: this remediation audit originally stopped at
  **M2 IMPLEMENTATION CONDITIONAL PASS**. The author subsequently accepted the
  remediation and formally approved the M2 exit gate at
  `eec2c1fd173748f181dc8b672b3be320035426a8`. M3 remains separately gated,
  unauthorized, and not started.

## M2 Author Exit Approval -- 2026-08-03

- Decision: **M2 REMEDIATION ACCEPTED** and **M2 EXIT GATE FORMALLY
  APPROVED**.
- Approved implementation HEAD:
  `eec2c1fd173748f181dc8b672b3be320035426a8`.
- Accepted evidence: the four Schema/Python parity corrections, raw-byte
  non-finite JSON loader regressions, 252-test cumulative suite, static and
  structural checks, frozen-digest verification, and independent reviews
  recorded above.
- Research boundary: this approval accepts M2 engineering evidence only. It
  creates no C2-C5 empirical result, TER/PIP/BSR observation, baseline
  advantage, or cross-agent generality conclusion.
- Scope boundary: CCS-2.1, the frozen execution plan, the frozen analysis
  protocol, code, Schema, tests, dependencies, and lock file remain unchanged
  by the approval record. M3 is not authorized and has not started.
