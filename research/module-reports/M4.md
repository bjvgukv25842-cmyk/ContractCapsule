# M4 Module Report

## Independent Exit Audit (2026-09-08)

**REQUEST CHANGES; awaiting item-by-item author repair approval.** Native
independent reviewer dispatch worked on this run; the historical tooling blocker
below is no longer current. The audit itself is complete, but M4 did not pass.

At exact technical2ce1653/governancecd6cfcf, independent verification reproduced
235 frozen/745 full/30 formal passes, static/type/complexity/lock/schema/build
gates and70/70 file discovery. Ten targeted mutations were caught. Independent
extra diagnostics still expose three blockers not covered by the745 tests:

- M4-R1 (P1): denied declaring-owner occurrence can contribute a graph edge
  through another admitted owner of an identical atom.
- M4-R2 (P2): a replacement ranker can override fixed P2/P3/P4 budget order.
- M4-R3 (P2): late revocation blocks content but retains denied identifiers in
  the public failure manifest.

Original Git locator binding and offline-read repairs now have scoped independent
PASS. Original foreign graph examples are fixed, but the residual owner-admission
variant prevents closing that finding. No production fix is authorized yet.

See `M4-2026-09-08-independent-exit-audit.md` for consolidated evidence, exact
reviewer reports, preserved failed diagnostics and three individually scoped
repair requests. This report-only update does not alter the audited technical
candidate or establish M4 exit approval. **M5 remains unstarted.**

## Latest Verified State (2026-09-06)

**Implementation and controller verification delivered; independent exit audit
and author approval are still pending. M4 is not formally complete.** This
section supersedes the historical preparation/checkpoint statuses below.

The reproducible technical candidate is
`2ce16535ece97733404e91715bb2b7586b54de20` on `codex/m4-view-compiler`.
It includes the service repairs at `ed7feae`, real compiler integration at
`ccabe4e`, and the separate Schema inventory adjustment at `2ce1653`.
The approved baseline remains M3 `4dbf484`; the isolated branch starts at its
governance-only approval descendant `9743b34`. ADR-0004 is accepted.

### Delivered Functionality

- Immutable runtime request/view/manifest models, explicit service protocols,
  Registry revalidation and default-deny admission before any ranking.
- Anchored POSIX scope matching, explicit authorization/freshness snapshots,
  collective interface coverage, deterministic in-memory FTS5/BM25 ranking.
- Atom and whole-capsule mandatory closure, exact consumer-owned release/digest
  locks, SemVer constraints, duplicate-content and conflict rejection.
- Offline hash-locked tokenizer and neutral renderer; complete-text accounting
  with boundary adjustment, mandatory P0/P1 closure, atomic optional groups.
- Native CAS, approved local Git and configured external snapshot evidence;
  expansion reauthorization, exact source/span verification and Secret scanning.
- Real ViewCompiler, independent B-zone View Manifest schema, complete admitted
  decision records, participating-provider witnesses and locked byte replay.

Source and test inventory is in `M4-verification/identity.json`; detailed
integration behavior, file ownership and RED/GREEN history are preserved in
`M4-compiler-implementation.md`. No M5 validator/activation/rollback or M6
adapter has been implemented.

### Verification at the Exact Candidate

The controller extracted clean Git archives of the exact candidate and M3
baseline, with source-first imports and caches outside the source archive.
This is independent of the working copy, but **not independent of the authoring
agent** and is not called an independent review.

| Check | Observed result |
|---|---|
| Frozen four-file pytest, `-v`, clean archive | 235 passed, 16.54s, exit 0 |
| Full pytest, clean archive | 745 passed, 28.66s, exit 0 |
| Formal fixtures | 30 passed, exit 0 |
| Original M3 test-node retention | all 339 retained; 406 additional nodes |
| Ruff normal and no-ignore scans | exit 0; all 70 Python files discovered |
| C901/PLR0911/PLR0912/PLR0915, both scans | exit 0; thresholds unchanged |
| Full Mypy | 70 source files, exit 0 |
| `uv lock --check --offline` | 46 packages, exit 0 |
| Offline sdist/wheel build and extracted-wheel smoke | exit 0; exact token count verified |
| Permission/closure/P0/P1/evidence/replay mutations | six faults detected by actual assertion failures |
| Frozen files, M1 model and eight core Schema bytes | unchanged from approved baseline |

Commands, exit statuses, complete failure/success logs and source hashes are
retained in `M4-verification/`. The six mutation probes are disposable process
patches, not repository mutations or experimental observations. P1's first
accounting-zero probe failed during budget fixture setup; it was replaced by a
targeted P1-overflow bypass plus forged final accounting. The corrected probe
actually returns an unblocked view and the expected blocker assertion fails.
Both probe records are retained, not counted as two successful fault detections.

The first archive run used macOS `/tmp`, whose symlink spelling correctly
triggers strict tokenizer path rejection (3 failed, 67 setup errors). Resolving
the verification harness root to `/private/tmp` produced the clean results above
without changing runtime security behavior. Those initial logs are preserved.

### Review Findings and Remaining Gate

The independent service review at `73f99ae` identified three P1 issues:
foreign graph-edge declaration ownership, Git locator/approved source-map drift,
and promisor Git lazy fetching. The exact report and reproducer are preserved
in `M4-services-independent-review.md` and `M4-services-review-diagnostic.py.txt`.
Repair `ed7feae` adds nine regressions and addresses those code paths; detailed
RED/GREEN evidence and limitations are in `M4-service-repairs.md`.

**These repairs have not received a new independent approval.** The integrated
compiler also awaits its first independent exit audit. The original reviewer
and implementer returned their reports, but the continuation tool surface did
not expose a callable follow-up/dispatch operation during final handoff. No
external model CLI, user-facing task or unofficial plugin was used as a bypass.
`M4-final-audit-handoff.md` supplies exact targets and pending review obligations.

The only original assertion adjusted is the Schema directory inventory, now
allowing the explicitly planned ninth B-zone document. The eight original
Schema identity comparisons and original test node remain intact. This narrow
authorized inventory adaptation is disclosed, not described as byte-identical
preservation of every old test file.

### Research and Exit Boundary

This delivers bounded C2 engineering evidence, RQ1 token instrumentation and
RQ2 selection/provenance traces, with C5 reproducibility records. It establishes
no formal benchmark result, TER/PIP/BSR measurement, superiority, cross-agent
generality, or G1 vertical slice. Local exact source mappings and explicit policy
snapshots are not online freshness, a Python sandbox or distributed atomicity.
An unusable admitted candidate's evidence conservatively blocks compilation even
when that candidate would ultimately be irrelevant.

Approved module progress is **4/12 (33.3%)** for M0-M3. M4 author approval would
make it 5/12 (41.7%), but must follow independent audit. Original G1/G2 dates
are missed and G3 is not passed; no frozen schedule or gate is rewritten.

Both frozen SHA-256 values still match:

- CCS-2.1: `aaddaa8def8df1ef2efbe4f9487de0ba86a60ae5aed8955ca76f5e070fa01e5c`.
- Plan: `7c1feebb36776c09de5c0ba5462540072374b9902d8e04d405585a8efb8659c0`.

Next action: independent read-only service repair and full compiler audit;
resolve any findings within M4, then request the author's M4 exit decision.
**No merge/push. No formal experiments. M5 has not started.**

## Resume Checkpoint (2026-09-06)

M4 is not complete. At resume HEAD
`73f99aebe519e3b0857c9df5fbbeae99e06ff68c`, graph resolution, local token metering,
neutral rendering and native evidence services exist, but the integrated
ViewCompiler, standalone B-zone schema, final reproduction/fault-injection
acceptance and independent exit audit remain outstanding.

| Increment | Technical commits | Recorded verification |
|---|---|---|
| two-level graph/provider resolution | `211ffa0`, `9dc9112` | 85 new focused /621 cumulative; Mypy55; full scan/complexity/lock/hash passed |
| offline counter and neutral renderer | `396a9f4` | 40 new focused /661 cumulative; Mypy59; full scan/complexity passed |
| native evidence and reauthorization | `73f99ae` | 18 new focused /679 cumulative; Mypy61; full scan/complexity passed |

The graph uses separate presence and whole-unit nodes, exact consumer-owned
release/digest locks and explicit root-provider membership. Unknown untyped
target names remain missing obligations, not guesses from punctuation. Genuine
mutual digest-lock cycles cannot be manufactured as valid content-addressed
fixtures; pure cycles and real same-capsule/mixed atom cycles are tested.

Counter resources are local and hash-locked. Complete rendered-text counts
include nonadditive boundary adjustment. A real extension-retention regression
failed before the renderer preserved signed Atom extensions; preserving those
fields may increase token cost and is not hidden compression. Native evidence
uses real Registry authorization and content/span checks for CAS, exact local
Git and explicitly configured external snapshots. The initial Git fixture
lacked collector proof and was fixed to traverse the real trusted M3 path;
production trust enforcement was not weakened. An unchanged-excerpt/changed-
full-blob fault was caught, and a digest-bypass mutation made that test fail.

Independent-review status was explicitly incomplete after agent tools became
unavailable. Tasks5/6 were implemented locally; self-review was not represented
as independent approval. Earlier invalid `functions.wait` calls with agent IDs
were controller tool errors, not useful waits, source changes or evidence.
At this resume, internal agent dispatch is available again: a fresh reviewer
is checking Tasks4-6 and a separate implementer is completing Task7. No M5,
external model CLI, merge/push or formal experiment is authorized or performed.

Detailed earlier scratch reports remain in
`.superpowers/sdd/2026-09-05-m4-view-compiler-execution-plan/task-{4,5,6}-report.md`.
Their intermediate test counts are historical observations, not a claim that
the full M4 exit already passed. Baseline hashes match and source worktree was
clean at resume. Final results will supersede this status with exact commits.

## Implementation Checkpoint: Contracts and Local Admission (2026-09-06)

M4 remains in progress. ADR0004 is accepted; models/protocols and local
admission have passed scoped independent reviews. This is not a complete
compiler, real view, M4 exit or M5 authorization.

| Increment | Commits | Evidence |
|---|---|---|
| model/protocol foundation | `53c6a23`, `036467c` | 54 new focused tests; 393 accumulated; full Mypy41; review PASS |
| local admission/policy/freshness | `8e0d1cc`, `9531920` | 105 focused; 498 accumulated; full Mypy47; scoped review PASS after one repair round |

The model increment adds immutable request/view/manifest/evidence/budget data,
stable serialization and explicit service protocols; it adds no placeholder
compiler. Request projection preserves A-zone plus detached-signature input,
excluding B/C, publication transport time and expected-output digest.

Admission revalidates Registry publication projection, enforces coherent
explicit grants, strict boolean/default-deny decisions, sensitivity, scopes,
dates/TTL, trusted freshness snapshots and local interface compatibility.
Internal admitted items retain full original payload for later provider checks;
only admitted atom IDs may reach ranking. Actual compiler/ranker integration,
native evidence verification and collective closure remain unimplemented.

Initial REDs included missing-module collection errors. Real assertion REDs
also exposed strict-float coercion, grant-snapshot normalization and malformed
selector handling. The local-admission reviewer found two actionable issues:
capsule and atom scopes could match different paths of a multi-path task, and
forged non-string identity keys could escape with TypeError. Real Registry
regressions produced 8 failures; correction produced 13 new passes. Both were
marked addressed by the same scoped reviewer. Original policy snapshot/task
bindings remain unchanged while only scope evaluation narrows the path set.

Main-agent full regression after that correction: 498 passed in 12.87s;
full no-ignore C901/PLR0911/PLR0912/PLR0915 checks pass. No original baseline
test is removed. Detailed ignored worker reports and review packages remain in
`.superpowers/sdd/2026-09-05-m4-view-compiler-execution-plan/`.

Preserved execution errors: Task1's initial src-only Mypy did not validate new
tests; a subsequent complete run found and fixed 37 test typing errors (final
Mypy41). Task2's initial Ruff `--no-ignore` flag was invalid; the required
`--no-respect-gitignore` checks were subsequently run. Source-side arbitrary
service catches use narrow documented BLE001 justifications, not global
suppression. Task1's SSE report delivery disconnected; the existing commits
and report were verified and reused, not reimplemented. Native web query
failure and tokenizer extraction-output contamination are recorded in the AI
ledger; no alternate search plugin was used.

The date is now 2026-09-06: G1 and the original G2 date are missed and G3 is
not passed. No frozen deadline or study design has been rewritten.

## Current Status

Task3 ranking checkpoint: `60b45eb` plus strict-input repair `6bbcb92`, independently
reviewed spec PASS / quality APPROVE. Final38 rank cases and536 cumulative tests
reported passing. Real literal-query FTS5/BM25 is in place; immutable service
metadata Protocol correction is covered by full Mypy consumer checks. Six
malformed nested-input regressions were RED before the repair and GREEN after.
Graph assembly, real budget/evidence/compiler integration and final M4 audit
remain outstanding; ranking alone does not prove unauthorized-ranker isolation.

**ADR-0004 AUTHOR-APPROVED; M4 IMPLEMENTATION IN PROGRESS.**

The author accepted proposal commit `7eec393` and ADR digest
`be057b3c85dd72c59f13193097b41b96a57a87f55408fcf71dbaf3cd6c2e46e9`.
M4 runtime implementation is now authorized. The preparation-stage details
below remain historical evidence; their pending-ADR wording does not override
decision M4-003. No M4 exit or M5 authorization is inferred.

The author approved M3's exit at `4dbf4849c6e125d1cb5ac8d813e4aeb5dc75694d`
and authorized M4 startup. M3's governance-only approval record is
`9743b34168e160878b9b9e8132512c540954f511`; the M4 branch starts at that exact
descendant, with no code change between the approved baseline and branch point.

- Branch: `codex/m4-view-compiler`.
- Worktree: `.worktrees/m4-view-compiler` under the existing repository root.
- Scope: Resolver and View Compiler, not M5 behavior/activation/rollback.
- Runtime implementation tasks M4-1 through M4-7: not started.
- Formal experiments, benchmark labels and comparative results: not started.
- No merge or push. **M5 has not started.**

## Alignment and Governing Inputs

The main agent fully reread AGENTS.md, the complete 888-line CCS-2.1 and the
complete 1308-line frozen execution plan, then checked the latest M3 decisions,
M1 Definition 3 and the corresponding interface gate in `formal.py`.

ContractCapsule is an immutable, evidence-grounded, contract-bearing context
replacement unit. M4 creates a task-specific derived view; it is neither a
replacement source of truth nor an independent compression/RAG novelty claim.

All seven core modules remain unchanged: Control Manifest, Semantic Payload,
Evidence Plane, Dependency Graph, Replacement Contract, Compression Policy,
Tests & Integrity. The twelve frozen invariants remain binding. M4 directly
implements reproducible views, admission-before-ranking, mandatory closure
and P0 retention; it preserves evidence/identity/trust foundations and leaves
safe-boundary, irreversible-action and activation gates to their planned M5/M6
owners. No invariant is claimed implemented merely because this report exists.

Required `superpowers:executing-plans` is available and read. Worktree and
documentation/ADR instructions were used for this preparation. Independent
internal review checks the proposal, not an implemented compiler or author
approval. No unavailable skill or external service is claimed as used.

## Preparation Deliverables

| Artifact | State |
|---|---|
| M3 approval record | committed before creating M4 worktree |
| isolated M4 baseline | created at exact approval-record descendant |
| derived M4 execution plan | saved with original module scope, tasks and gates |
| ADR-0004 | Proposed; requires author approval before semantic implementation |
| M4 runtime models, resolver, compiler and B-zone schema | not implemented |
| new M4 tests and dependency additions | not implemented or installed |

The derived plan is
`docs/superpowers/plans/2026-09-05-m4-view-compiler-execution-plan.md`.
The proposed ADR is `docs/adr/0004-m4-collective-interface-coverage.md`.
Neither replaces or edits a frozen file.

## Selected Direction and Current Gate

The author selected collective task-interface coverage with prior ADR approval,
minimal local `tiktoken`/`semantic-version` dependencies, and release-version
constraints independent of exact `/vN` interface names.

ADR-0004 separates per-capsule local security/compatibility admission from
collection coverage before ranking; it requires exact provider witnesses and
final selected-provider preservation. M1's original model and tests stay intact,
with a separate M4 runtime profile and singleton interface-gate compatibility
obligations. No proposal statement is treated as an already verified M4 result.

The internal fresh-context reviewer `m4_adr_preimplementation_review` found two
actionable approval-stage ambiguities: interface-to-payload membership and the
authority/ownership of provider-selection locks. Both were corrected in the
Proposed ADR: task interfaces require the provider's whole payload and closure;
root interfaces require one unique usable provider in the exact request input;
capsule dependencies use only their consumer's own exact lock. The output
witness does not authorize its own initial choice. Full-payload token costs are
explicitly disclosed, with no new fine-grained membership field or mapping.

The same reviewer checked the amendments, marked both findings addressed and
found no new explicit issue in the scoped revision. This is document review
only: no new runtime test was run, no author approval is inferred, and no
cross-model or external-CLI review was performed.

The next author decision is whether to accept ADR-0004's concrete text.
Dependency version/resource verification, code, schemas and semantic tests
remain behind that gate. Future package versions are not invented here.

Exact proposed ADR SHA-256:
`be057b3c85dd72c59f13193097b41b96a57a87f55408fcf71dbaf3cd6c2e46e9`.

## Commands and Observed Baseline

| Command/check | Exit and observed result |
|---|---|
| M3 `git status` / `rev-parse HEAD` before approval record | clean; exact approved `4dbf484` |
| full M3 baseline `uv run pytest -q -ra` | Exit 0; 339 passed in 8.41s |
| M3 governance spec-lock/ledger tests | Exit 0; 5 passed |
| M3 approval-record protected-file diff / whitespace | Exit 0; no code/test/schema/dependency/frozen change |
| root `.worktrees/m4-view-compiler` ignore check | Exit 0; matched existing `.worktrees/` rule |
| branch/directory existence checks | no preexisting M4 branch or destination |
| `git worktree add -b codex/m4-view-compiler ... 9743b341...` | Exit 0; new worktree at exact governance commit |
| `uv sync --locked --offline` in M4 | Exit 0; existing 38-package lock resolved, local environment prepared |
| M4 baseline `uv run pytest -q -ra` | Exit 0; 339 passed in 11.49s |
| M4 documentation-stage `uv run pytest -q -ra` | Exit 0; 339 passed in 8.49s |
| complete no-ignore complexity and whitespace checks | Exit 0 |
| frozen SHA-256 checks | both approved values match |

The test command yielded while still running and was explicitly polled to
completion; only its final Exit 0 is the evidence above. No M4 feature test,
mutation or runtime experiment is reported as executed at this stage.

Dedicated native worktree creation was not available: only task/thread
creation and handoff tools matched discovery, which were outside this request.
The authorized Git worktree fallback was used; no new user-facing task was made.

## Evidence and Scope Boundaries

This phase records C1 interpretation/compatibility planning and C5 governance
provenance. C2 M4 implementation, RQ1 token metering, RQ2 selection traces,
RQ3 replacement outcomes and RQ4 cross-agent results remain absent. It adds no
C3 benchmark or C4 empirical comparison. AI decisions and actions are recorded
in the append-only ledger, with M4 human validation pending.

The M4 frozen window was 2026-08-17 through 2026-08-21. Startup on 2026-09-05
is after that window. G1 is missed; G2 is due today without its prerequisites;
G3 is tomorrow. No deadline or gate is rewritten or marked passed.

Frozen SHA-256 values:

- CCS-2.1: `aaddaa8def8df1ef2efbe4f9487de0ba86a60ae5aed8955ca76f5e070fa01e5c`.
- Execution plan: `7c1feebb36776c09de5c0ba5462540072374b9902d8e04d405585a8efb8659c0`.

**Stop condition now: ADR-0004 author approval. M4 is not complete. M5 unstarted.**
