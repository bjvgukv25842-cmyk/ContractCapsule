# M5 Module Report

## Latest recovery status (2026-09-13)

The M5 worktree is restored. The author-approved Docker response repair and
its recursion residual are independently reviewed PASS at `d5ad681`.
Five new regressions demonstrate stable failures and exact cleanup; full979
tests pass. M5-4B remains incomplete and its source helper still fails two
complexity rules. M5-5/6/7 and author exit remain. See
`M5-2026-09-13-resume.md` for exact recovery provenance, checks and limitations.
Sections below are historical checkpoints, not current authorization requests.

## M5-3 Repair Passed; Docker Task Started (2026-09-10)

The author approved M5-3-R1. Repairf94412d requires disjoint container identities
within the complete old/new/pair check and probe collection and reuses that
check during persisted report/group verification. It adds10regressions; full938
tests pass. Independent scoped rereview marks the finding ADDRESSED and
Task3 specification/quality PASS, preserving valid cross-repetition reuse.
Original failed review remains historical evidence. See the appended fix in
M5-3-implementation.md and M5-3-R1-independent-rereview.md.

M5-4 real Docker paired execution is now in progress, not completed. Task1-3
are reviewed; Task4-7 and M5 author exit remain. No M6 or formal experiment.

## Current Stop: M5-3-R1 Repair Approval (2026-09-10)

M5-1 andM5-2 are implemented and independently reviewed PASS. M5-3 at20cb819
passes71new/928cumulative implementation tests, but independent review returns
REQUEST CHANGES for one P2 paired container-identity consistency gap. Reusing
container IDs across old/new executors or a subject/pair checker in the same
pair can produce a valid authenticated report. This is synthetic record
validation evidence, not an actual Docker escape.

See M5-3-independent-review.md and M5-3-repair-request.md for exact evidence
and the narrow proposed repair. The controller confirmed the actual assertion
failure and archived the output. No fix has been applied; the author's existing
itemized repair gate requires approval of M5-3-R1. Task4 is not started, M5 is
not complete, and M6/formal experiments remain unstarted.

## M5-3 Implementation Checkpoint (2026-09-10)

Task2 now has independent specification/quality PASS. Task3 is implemented at
20cb819c19c6c4c6edf632ba1d6d21bc0e53fffd: authenticated behavioral observations,
separate TER/PIP/BSR, complete source-relative deltas and paired/repetition
reports. Seventy-one new tests pass; cumulative928. Independent Task3 review
is in progress. These are synthetic host-recorded observations, not Docker
execution or formal experimental results. Task4 runner and Tasks5-7 remain.
See M5-3-implementation.md for exact capture shapes, stage inputs and limits.

## M5-2 Implementation Checkpoint (2026-09-10)

Task1 has scoped independent spec/quality PASS. Task2 now exists at c4fa931
and917e702: authenticated same-Registry report journal, independent direct
core/member/evidence/render/token verification and current admission gates.
It adds48 tests, cumulative857 pass. Independent Task2 review is in progress;
no Docker run/behavior result/action pointer/activation is claimed yet.
Detailed APIs, real failures and verification are in M5-2-implementation.md.

## M5-1 Recovery Checkpoint (2026-09-10)

Task1 now has real implementation at bbf031d8d83cfdf055ab6fc1a4aa617c7c8fc614:
strict executable bindings, real Registry/package-byte verification and a
separate local approval authority. Forty-six new tests pass and cumulative
regression is809. Independent task review is in progress; this is not M5 exit
or completion of Tasks2-7. Detailed implementation/TDD and interfaces are in
M5-1-implementation.md. The abandoned first worker's unfinished test was
preserved; it was replaced by genuine approved publications and correct byte
digest fixtures. No result from that incomplete test is counted as completion.

Acceptance provenance is corrected by the append-only ledger record linked to
M5-003. The old record incorrectly used an ADR file hash as a commit and an
incorrect prompt hash; historical bytes remain auditable. The accepted ADR
itself and its original user authorization remain valid.

## Current State: Preparation, Not Implementation Completion

Date: 2026-09-09. M5 startup and ADR-0005 exact text are author-authorized.
M5-1 execution may now begin. No M5 production code, runtime table, validator,
behavioral evaluator or swap controller exists yet; no M6 or formal experiment
has started.

The initial document review returned REQUEST CHANGES for four ambiguities.
Revised proposal `2842a13ddc3e1bb29479017a50e4eae5ad6f239b` addresses all four;
the same independent reviewer returned READY FOR AUTHOR APPROVAL. The author
then approved that exact text on 2026-09-09; acceptance metadata is recorded in
the ADR successor. Document review is not a runtime behavior result.

M4 author approval is durably recorded in commit
`3bcdbc638de4c10e30fc7dee666e2517450bf1f5`, after approved governance1aeeb11 and
technicalbb1d56d. The M5 worktree `.worktrees/m5-validation-swap` and branch
`codex/m5-validation-swap` start at that exact approval commit. All earlier
worktrees and technical source are preserved; no merge/push was performed.

## Alignment

The main agent fully read AGENTS, complete CCS-2.1 and the complete frozen
execution plan, then the current M4 approval/audit records and relevant M1/M2/M4
interfaces. Both approved frozen hashes match. Required executing-plans skill
is present and read, together with worktree, ADR and review/verification guidance.

ContractCapsule is an immutable evidence-grounded unit whose replacement must
be behaviorally validated. M5 operationalizes that distinction from summary/RAG
through separate target/invariant/spillover checks, trusted run evidence and
atomic state change. It retains all seven core modules and twelve invariants;
in particular it owns the safe-boundary and activation gate implementation,
not a new source of truth or an Agent adapter.

The author selected signed-core x-* execution bindings and Docker isolation.
The new ADR states how to map textual clauses, M1 accepts_interfaces and actual
test programs without executing untrusted strings or accepting fabricated
passed flags. Identity, publication authority, execution approval, run evidence
and activation authority remain distinct boundaries.

## Deliverables So Far

- M4 exit/M5 start approval record, before the isolated M5 branch.
- Proposed `docs/adr/0005-m5-executable-contracts-and-swap.md`.
- Derived `docs/superpowers/plans/2026-09-09-m5-validation-swap-execution-plan.md`.
- Fresh baseline regression and bounded local Docker profile preflight.
- Initial independent document review at198616e and bounded rereview at2842a13,
  both preserved. Revised text is author-approved as ADR-0005.

The seven post-approval tasks remain: executable bindings, independent view
validation, behavior/differential evaluation, real Docker twin execution,
durable runtime state/boundaries, activation/rollback, and integrated exit audit.
The derived plan contains actual test files, frozen command and stopping gates.

## Observed Commands and Results

| Command/check | Actual result |
|---|---|
| Existing M4 baseline `uv run pytest -q` | 763 passed,32.94s |
| M4 approval-record ledger/hash tests | 5 passed,0.10s |
| M5 `uv sync --locked --offline` | exit0;46 resolved packages, unchanged lock |
| New M5 worktree baseline `uv run pytest -q` | 763 passed,37.83s |
| Docker version and local image inspection | daemon available; client/server29.5.2, Linux/arm64 image |
| Bounded Docker profile probe | exit0 after correcting an invalid interface-inventory assumption |
| Source/test/schema/dependency diff across approval commit | empty |
| Proposal whitespace check | exit0 |

The local image is locked by content ID:
`sha256:090ba77e2958f6af52a5341f788b50b032dd4ca28377d2893dcf1ecbdfdfe203`.
Container Python reports3.12.13. The successful probe verified Docker network
mode none, no host binds/mounts, read-only root, UID65534, zero effective
capabilities, no-new-privileges, no Docker socket,1GiB memory,128processes and
CPU quota100000/100000. It observed no IPv4 routes and no enabled non-loopback
interface. No image was pulled, no user directory/credential was mounted and
no external network connection was attempted.

The first probe failed an over-strict expectation that only a lo name exists;
the second printed evidence showing disabled kernel tunnel templates. The
third used real interface-up flags plus route/Docker configuration checks,
without loosening any Docker isolation setting. All failed outputs/commands
are retained. This is environment feasibility evidence, not a finished M5
sandbox test or a proof against kernel/privileged-host compromise.

Only the three disposable probe containers were removed (two by --rm, one by
its exact validated container ID). They held no persistent user data. The
local image and all user containers remain untouched.

## Research Boundary and Remaining Decisions

This preparation contributes design traceability to C1/C5 and plans C2/RQ3
engineering evidence. The763 existing tests are inherited M0-M4 regressions;
they are not M5 feature tests or an empirical replacement experiment. No
target-effect/invariant/spillover outcome, active pointer or rollback success
has yet been produced by M5.

M0-M4 formal completion is5/12(41.7%). M5 is in progress, not6/12. Its frozen
window ended2026-08-24; G1/G2/G3 dates are past and their gates remain unpassed.
Continuing M5 does not certify the original submission calendar. Structural
scope/schedule revision requires a separately approved ADR and cannot weaken RQ3.

Next gate: implement M5-1 under accepted ADR-0005. Each execution still needs
its own bound approval and validated artifact; module-start approval is not
permission to execute arbitrary capsule programs.
Implementation audits retain the author's itemized repair-approval rule.

Independent reports: `M5-ADR0005-review-initial.md` and
`M5-ADR0005-rereview.md`. The revised ADR SHA-256 is
`da92462f397a82a6d5c2a2320b6ab98280f5af69edc538ed09ac6533e3115687`.
The revised plan SHA-256 is
`afaf6ebd8d8edc2c16afcff222c5389111e984e3cc22749def15e27deee26b83`.
Raw startup/probe failures and successful runs are in `M5-startup-evidence.tar.gz`.
Post-proposal regression also passed763 tests in33.20s; Ruff no-ignore and
Mypy72 pass. These remain inherited M0-M4 checks, not M5 feature tests.

Frozen hashes:

- CCS-2.1: `aaddaa8def8df1ef2efbe4f9487de0ba86a60ae5aed8955ca76f5e070fa01e5c`.
- Plan: `7c1feebb36776c09de5c0ba5462540072374b9902d8e04d405585a8efb8659c0`.

**M5 not complete. M6 has not started.**
