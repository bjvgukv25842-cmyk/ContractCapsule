# ContractCapsule 项目宪法

## 1. 项目身份

本仓库用于研究、实现和评估 `ContractCapsule v2.1-Frozen`（规范编号
`CCS-2.1`），并以向 **FSE 2027 Research Papers** 投稿一篇可复现的研究论文
为唯一一级目标。

工作论文题目：

> ContractCapsule: Verifiable Context Replacement for Coding Agents

项目的核心问题不是“如何保存更多上下文”，也不是“如何把文档摘要得更短”，
而是：

> 能否把项目上下文构造为不可变、带原文证据、受行为契约约束的替换单元，并在
> 旧、新上下文切换时验证目标效果、保护不变量和行为外溢？

插件、CLI、MCP、Skill、Codex/Claude 适配器均是论文研究载体。除非已经进入
冻结计划中的相应模块，否则不得把产品化功能置于论文证据之前。

## 2. 最高依据与文件优先级

开始任何分析、编码、测试、实验或论文写作前，必须完整读取以下两份文件，不能
只依赖本文件的摘要：

1. 设计语义的唯一规范：
   `docs/spec/CCS-2.1.md`
2. 工作顺序、研究设计和停止门：
   `docs/superpowers/plans/2026-07-30-contract-capsule-fse-2027-execution-plan.md`

首次初始化时，如果规范尚未位于上述路径，可以在仓库中寻找
`contract-capsule-v2.1-frozen-spec-cn*.md`；如果计划尚未位于上述路径，可以寻找
`2026-07-30-contract-capsule-fse-2027-execution-plan*.md`。找到后先校验摘要，再
按 M0 要求复制到规范路径，不得重排、翻译或格式化原文。

冻结文件摘要：

- `CCS-2.1` SHA-256：
  `aaddaa8def8df1ef2efbe4f9487de0ba86a60ae5aed8955ca76f5e070fa01e5c`
- 模块化执行计划 SHA-256：
  `7c1feebb36776c09de5c0ba5462540072374b9902d8e04d405585a8efb8659c0`

如果任一摘要不匹配，立即停止，不得基于“看起来相似”的文件继续。

项目内部规则冲突时，按以下顺序处理：

1. `CCS-2.1` 决定胶囊结构、语义、安全边界和十二条不变量；
2. 冻结执行计划决定 M0–M11 的顺序、范围、研究设计、交付物和停止门；
3. 本 `AGENTS.md` 决定 Codex 的日常执行纪律；
4. 代码注释、README、临时笔记和派生文档不得反向覆盖前三者。

如果规范与执行计划存在真实冲突，不得自行挑选方便的一方。记录冲突、影响范围
和建议处理方式，然后停下等待作者决定。

`CCS-2.1` 和冻结执行计划是只读基线。不要在其中勾选进度或直接修订内容。执行
状态写入 `research/decision-log.md` 和 `research/module-reports/`。结构性变更
必须先形成 ADR，说明兼容性、实验与安全影响，经作者批准后发布新的规范或计划
版本；不得就地改写冻结版本。

如果当前工作目录已经包含本 `AGENTS.md` 和 `docs/`，它就是目标仓库根目录。
此时执行 M0 时直接在当前目录初始化，不要机械执行计划示例中的
`mkdir -p contract-capsule && cd contract-capsule`，以免创建嵌套仓库。只有从
目标仓库的父目录启动时，才创建 `contract-capsule/`。

## 3. 不可改变的设计结论

### 3.1 双轨原文策略

- 保存轨：授权范围内的原文全量、无损、不可被摘要覆盖地保留，或使用不可变
  `commit + path + digest` 引用。
- 使用轨：默认只向 Agent 注入任务相关语义原子、必要原文摘录和可展开证据句柄。
- 摘要只是可重建的运行视图，不是事实源。
- 无损存储压缩不会减少模型输入 Token，不得把它描述成运行时上下文压缩。

### 3.2 三个区域

- A 区：不可变签名核心，决定胶囊身份；
- B 区：Embedding、索引、缓存和 Agent 视图等可重建派生物；
- C 区：轨迹、观察和隔离区等可变运行侧车。

B、C 区不得反向覆盖 A 区，也不参与胶囊事实身份。

### 3.3 七个核心模块

任何实现都必须保留：

1. Control Manifest；
2. Semantic Payload；
3. Evidence Plane / source-map；
4. Dependency Graph；
5. Replacement Contract；
6. Compression Policy；
7. Tests & Integrity。

### 3.4 热、温、冷驻留

- CPU/GPU 是解析、闭包、排序、编译和验证的计算层，不是事实存储；
- 热层是本轮 Agent 真正读取的最小充分运行视图；
- 温层是可重建索引、锁、缓存和已编译视图；
- 冷层是不可变核心、全量证据、历史版本、测试和审计。

### 3.5 P0–P4

- P0 必须精确加载，禁止静默删除或有损压缩；
- P1 只允许经验证的结构化规范化；
- P2 使用精确摘录和证据句柄；
- P3 可以生成有来源的摘要；
- P4 可以聚合、设置 TTL 或在预算不足时优先丢弃。

如果 P0 与强制依赖闭包已经超过预算，编译失败并要求拆分任务或更大上下文；不得
私自降低优先级。

## 4. 十二条硬不变量

以下规则必须分别具有可故障注入的测试：

1. 发布后的胶囊核心不可变；
2. 有损摘要永远不能覆盖原文证据；
3. 运行视图必须能从核心与锁文件重建；
4. Agent 新发现默认进入隔离区；
5. 权限、范围和新鲜度检查先于相关性排序；
6. 入选原子的强制依赖必须闭包；
7. P0 不能被静默删除或有损压缩；
8. 每个注入模型的正式原子都必须可追溯；
9. 胶囊替换只能发生在安全动作边界；
10. 不可逆外部动作必须有预检、审批或补偿方案；
11. Embedding、缓存和 Agent 视图不是胶囊事实身份；
12. 完整性、安全或行为契约失败时必须阻断激活。

安全、权限、证据、依赖、完整性和行为契约采用 fail-closed。只有纯观察性遥测
故障可以按规范有限降级。

## 5. 论文优先原则

论文主张是“可验证的上下文替换”，不得把以下内容单独包装成论文创新：

- typed atoms；
- 上下文压缩；
- RAG、Embedding 或记忆分层；
- provenance/source-map；
- Skill、MCP 或 Agent 适配。

需要共同证明：

- `TER`：目标效果实现；
- `PIP`：保护不变量保持；
- `BSR`：禁止范围内的行为外溢受控；
- 强制依赖完整；
- 运行视图可追溯到不可变证据；
- 激活是原子的，旧版本可恢复。

`RCS = TER × PIP × (1-BSR)` 只能作为辅助描述，必须分别报告三个分量。

如果工作仍可被准确概括为“摘要加元数据”，立即触发新颖性停止门，不得继续以
代码量掩盖研究贡献不足。

## 6. 模块授权规则

- 严格按照 M0 → M11 执行；
- 每次只执行用户明确授权的一个模块；
- 未得到作者对当前退出门的批准，不得开始下一模块；
- 不得因为日程落后跳过停止门、测试、人工真值审核或失败分析；
- 不得在 G2 协议冻结前开始正式实验；
- 不得把基础设施重试用于替换有效但不利的实验结果；
- 不得为维持论文主张而删除、改写或补造数据。

首次启动默认只允许执行 M0。若用户只要求理解、审计或报告状态，则保持只读，
不得自动开始实现。

三个绝对停止门：

1. 新颖性门：贡献与压缩/RAG/普通记忆/Atom Only 无法区分；
2. 生存门：垂直切片、客观真值、条件隔离、稳定性或预算不成立；
3. 投稿门：引用、匿名、复现、页数或数据追溯任一失败。

## 7. 每个模块的固定执行循环

执行模块前：

1. 完整读取冻结规范、冻结计划、当前模块和最新决策日志；
2. 检查冻结计划声明为必需的 Skill 是否实际可用；不得声称加载了不存在的 Skill；
3. 检查 Git 状态、现有文件和已有修改，保留用户工作；
4. 明确本模块输入、输出、测试、论文证据和退出门；
5. 生成简短的对齐报告，指出缺失输入、冲突和风险；
6. 若摘要、依赖、必需 Skill 或授权失败，停止。

执行模块时：

1. 先写能够失败的测试；
2. 做满足冻结接口的最小实现；
3. 运行模块测试和累计回归测试；
4. 不进行与本模块无关的重构、产品化或依赖扩张；
5. 记录来源、命令、输出、失败、Token/成本和版本信息；
6. 更新 `research/claim-evidence-matrix.md`；
7. 追加 `research/ai-usage-ledger.jsonl`；
8. 生成模块报告；
9. 停在人工退出门。

模块报告至少包含：

- 本轮授权模块和完成状态；
- 修改文件；
- 运行命令及退出状态；
- 通过与失败测试；
- 对应 C1–C5、RQ1–RQ4 和论文段落；
- 已知限制、偏离和未解决风险；
- 冻结文件摘要复核；
- 需要作者作出的决定；
- 明确声明“未开始下一模块”。

## 8. 研究诚信与 AI 使用

- Codex 不是论文作者；
- Codex 生成的原子、契约、测试、标签和解释默认不是人工真值；
- 每个基准任务的目标效果和保护不变量必须由人类作者依据原文确认；
- 至少 25% 的任务需要第二位独立审阅者；
- 任何影响研究设计、代码、数据集、实验、分析、图表或结论的 AI 使用均写入
  `research/ai-usage-ledger.jsonl`，并为论文 Methods 提供可披露记录；
- 引用必须回到真实、可验证的原始论文或官方文档；
- 禁止生成或保留虚构引用、虚构运行、虚构数据和无法追溯的数字；
- 负面结果、失败案例和有效的不利运行必须保留；
- 原始实验数据冻结后只读，任何修订生成新版本并保留旧摘要。

需要最新事实时可以进行只读网页查询，但优先使用 FSE/ACM 官方页面、原始论文和
Agent 官方文档。网页结果只能补充事实，不能静默改写冻结设计；发现外部证据要求
变更时，提交 ADR 或研究风险报告。

## 9. 工程范围

FSE 2027 投稿前不实现：

- 图形化胶囊编辑器；
- 插件市场发布；
- 分布式 Registry 共识；
- 生产 PostgreSQL/S3 部署；
- 组织计费和企业多租户；
- 任意向量数据库适配；
- T3 观察的自主晋升；
- 对供应商内部 KV Cache 的直接控制；
- 对不可逆外部行为的“完全回滚”；
- 未经人工验证的自然语言契约批量生成。

ContractCapsule Skill、MCP 和 Agent 适配器属于 M6。在 M6 前不得提前把整个系统
退化为一个 Skill；Skill 只教 Agent 如何调用统一协议，不拥有胶囊事实，也不内嵌
实时胶囊载荷。

## 10. 首轮输出要求

第一次收到启动提示词后，先输出“项目对齐报告”，至少回答：

1. 实际读取了哪些指令、规范和计划文件；
2. 两份冻结文件的 SHA-256 是否匹配；
3. 用一句话解释 ContractCapsule；
4. 说明它与“摘要 + 元数据”、普通 RAG 和 Agent 记忆的本质区别；
5. 列出七模块、十二不变量和当前授权模块；
6. 指出当前模块的交付物、测试和退出门；
7. 检查当前日期与冻结日程的偏差；
8. 列出任何阻断项。

对齐报告通过后，才可执行已授权模块。不要在首轮重复粘贴两份文件全文；用路径、
摘要和结构化结论证明已经读取。
